# Work orders for the implementation session

Each order is self-contained: what to build, the contract it implements, the evidence it rests on, and the artifacts the spec session will ask for before recording it as done. An order is closed only when its verification artifacts are in `90-observations.md`; the implementation session's own report is a claim until then. The implementation session does not write in `documentation/`; deliver artifacts by commit message, trace files outside this directory, or by handing them to the maintainer.

## WO-1 — `possibly_superseded` on `get_us_code_section` (R14a/b/c; contract in `40-tools.md`, "Staleness indicator"; measured basis O43)

**Status:** CLOSED 2026-09-15 — built at implementation commit 1d42c52, verified O44 (artifact review, black-box probe, and the two ratification measurements). Disposition of the order's own error: its premise that the detector is independent of the text fetch was wrong (the bound needs `currentthrough` from the fetched HTML); the implementation's cold/warm two-regime design is ratified and the contract corrected (O44c). Both implementation decisions submitted for ratification are ratified as interim behavior and superseded by WO-2 below: appendix `not_checked`/`no_measured_citation_form` stays as the fallback for appendix rules, and the private-law question is answered by measurement (O44e), not by leaving the query alone. The order text below is kept as issued, for the record.

**What it is.** Every successful `get_us_code_section` response carries a `possibly_superseded` object saying whether GovInfo's PLAW index lists any public law published after the returned edition's `currentthrough` date against this section. It is an indicator in both directions and never a certification (R14a): a fire does not mean the text is stale, and silence does not mean it is current.

**The detector query.** One `POST /search` against `collection:PLAW`, exactly this shape:

```
collection:PLAW publishdate:range({since},) uscodecitation:"{title} U.S.C. {section}"
```

- `{since}` is the response's own `currentthrough` plus one day, ISO `YYYY-MM-DD`, open-ended upper bound. `currentthrough` is the value already parsed from the granule HTML comment for provenance (O5, O15), so the bound is correct for `year`-selected editions too. Never hard-code `congress:119` or a fixed date.
- `publishdate`, never `approveddate`: `approveddate` ranges return HTTP 500 upstream in every form tried (O43f). The `publishdate` form was measured equal to the `congress:119` ground truth (104 = 104) and exact at the Congress boundary (O43f).
- `{title}` and `{section}` are the section actually resolved, after the mandatory strips (subsection parenthetical O11, trailing "note" O16). Citation string form is `"42 U.S.C. 2210"` — the form measured in O43c/O43d/O43f.
- Same authentication as every other call (R7, `X-Api-Key`).

**Three states on a `status` discriminator, never two.**

| `status` | when | must carry |
|---|---|---|
| `laws_indexed` | upstream 200, `count` ≥ 1 | true upstream `count`; `laws[]` of `{package_id, title, date_issued}`, capped, with the cap and the true total stated whenever capping happened (the disambiguation-totals rule); the fixed caveat that this indicates possible change, not that the text is stale |
| `none_indexed` | upstream 200, `count` = 0 | the fixed caveat that this is NOT a currency check and that absence of a match is not evidence the text is current — the index misses about one in seven listed (law, section) pairs (O43b) and one in four in an earlier sample (O21) |
| `not_checked` | anything else: non-200, HTTP 429, timeout, malformed body | the upstream HTTP status and body verbatim, or the local reason (timeout, parse failure); 429 named as rate-limit, distinct from other failures (the three-outcomes rule, `40-tools.md`) |

Every state, including `not_checked`, echoes the exact `query` string sent and the `since` date it was bounded by. When `stripped_note` is true on the lookup, the object states in-band that the check ran against the parent section and that a law affecting only the note may be indexed against neither citation (O17/O18, O43b).

**Concurrency and failure isolation.** Issue the detector query concurrently with the `txtLink` fetch, not after it; measured from the spec session's network position a keep-alive request costs ~200 ms and the detector 210–280 ms, so run in parallel with the text fetch it adds ~0 wall-clock in the common case (O43e). A detector failure of any kind never fails the lookup: the section text is returned and the object says `not_checked`. Bound the wait — if the detector has not returned within a small budget after the text is ready, report `not_checked` with reason `timeout`; do not hold the text for it. The budget value is the implementation's choice; state it in the report.

**Do not.** Retrieve or parse the listed laws to resolve effective dates (declined, R14a). Use `approveddate` ranges (O43f). Collapse `not_checked` into `none_indexed`, or omit the object on any success. Add an opt-in flag (Q13 closed it). Cache detector results across lookups without stating so — and if you do cache, the cache key must include `since`, because two editions of the same section have different bounds. Write anything under `documentation/`.

**Unit tests (repo rule: failure paths, not just the happy path).** Mock the upstream at the HTTP layer and cover at least: 200 with hits → `laws_indexed` with `count` and capping stated when hits exceed the cap; 200 with zero hits → `none_indexed` with the caveat text; 500 → `not_checked` carrying status and body; 429 → `not_checked` naming rate-limit; timeout → `not_checked` with `timeout`, and the lookup still succeeds with full text; malformed JSON → `not_checked`; `stripped_note` lookup → query built on the parent section and the in-band statement present; a `year`-selected edition → `since` derived from that edition's `currentthrough`, not the newest; `query` and `since` present in every state. Mocked fixtures are not evidence of upstream behavior — they prove the code's handling of each shape, which is what they are for.

**Verification artifacts the spec session will ask for** (traces, not summaries — the recorded request and the recorded response, from real upstream calls):

1. `42 U.S.C. 2210` → `laws_indexed`. Expected to list PLAW-119publ74 (O43d); ground truth also lists publ21, and the detector missing it is the measured instrument gap, not a defect.
2. `17 U.S.C. 107` → `none_indexed` (O43d: silent, and no 119th-Congress law lists it).
3. A forced upstream failure (bad key on the detector call only, or a stubbed 500) → `not_checked` with the status and body, and the section text delivered in full alongside it.
4. `42 U.S.C. 2210 note` → the `stripped_note` in-band statement, query built on `42 U.S.C. 2210`.
5. `17 U.S.C. 107` with `year: 2023` → `since` equal to the 2023 edition's `currentthrough` plus one day, so the bound is visibly edition-relative.
6. The exact query strings for 1–5, and the measured wall-clock of a lookup with the detector on versus off, n stated.

The spec session will then black-box the same five inputs against the running server (as it did for R12 in O37 and R13 in O39) before marking this order closed and adding the manifest checks.

**Out of this order.** E15's api-surface extension is harness-side (mcp-e2e), not this server. The A/D isolation-cell runs and Group F questions are spec-session and maintainer work.

## WO-2 — detector follow-ups from O44: `lawtype:public`, and numbered appendix sections (contract in `40-tools.md`, "Staleness indicator"; measured basis O44d/O44e)

**Status:** CLOSED 2026-09-15 — built at implementation commit faa9974, verified O45 (artifact review and black-box probe; the pinned caveat compares equal character for character). Two deviations from the artifact list accepted, one of them the order's own error: the tool has no by-id input, so "disambiguated by ids" was impossible — filed as Q14. The order text below is kept as issued, for the record.

**Change 1 — add `lawtype:public` to the pinned detector query.** New shape, exactly:

```
collection:PLAW lawtype:public publishdate:range({since},) uscodecitation:"{title} U.S.C. {section}"
```

Why: a live section fires today on a 119th-Congress private law — `10 U.S.C. 7274` → [PLAW-119pvtl2, PLAW-119publ60] on the WO-1 shape — and `get_public_law` refuses `pvtl` packages under R6, so the consumer would be handed an id it cannot retrieve. `lawtype:public` composes with the pinned shape, drops exactly the private package (O44e: 7274 → 1, 8298 → 2, 8300 → 1), leaves the public cases unchanged (42/2210 → 1, 20/1070a → 1), and partitions the collection exactly (60 + 5,939 = 5,999). `count` stays upstream's count for the query actually sent; the caveat text does not change. Everything else about the object is unchanged. Also a precision fix, not only a retrievability one: the private laws in the index cite sections to *waive* them for a named person ("Notwithstanding the time limitations specified in section 7274 of title 10 …"), never to amend them (O44f), so a private-law hit is a listing that cannot mean the section's text changed.

**Change 2 — run the detector for numbered appendix sections.** When the resolved granule is an appendix *section* (the citation normalized to `{title} U.S.C. App. {section}` with a numeric-led section, e.g. `18 U.S.C. App. 1201`), build the detector on the measured form:

```
collection:PLAW lawtype:public publishdate:range({since},) uscodecitation:"{title} U.S.C. App. {section}"
```

Measured basis (O44d): the field carries this form (`"50 U.S.C. App. 2012"` → 19 all-time, 1 since 2025-01-07; `"5 U.S.C. App. 3"` → 29; MODS witness on PLAW-119publ75). Appendix *rules* (`28 U.S.C. App. Rule N`, and any appendix citation whose qualifier is not a numbered section) keep the WO-1 fallback — `not_checked`, `reason: no_measured_citation_form`, `query: null` — because no form is measured for them (0 hits on `"28 U.S.C. App."` and `"28 U.S.C. App. Rule 9"`). The `checked_citation` field carries the App. form actually queried.

**Change 3 — pin the `laws_indexed` caveat to the contract wording.** Replace the current `laws_indexed` caveat with the text now pinned verbatim in `40-tools.md` ("INDICATOR ONLY — NOT A FINDING THAT THE TEXT CHANGED. A listed law MENTIONS this section … YOU MUST READ THE LISTED LAW TO FIND OUT — get_public_law with its package_id, then search its text for this section. …"). Basis: the maintainer's R14a addendum, and two measurements — private laws cite sections only to waive them (O44f), and the verification set's own fire, Public Law 119-74 against 42 U.S.C. 2210, is a single parenthetical citation in an appropriations rider with no amendment (O44g). The tool description's staleness sentence gets the same point in one line: a listing means the law mentions the section, and only reading the law says whether the text changed. The `none_indexed` and `not_checked` caveats are unchanged.

**Do not.** Change the three-state shape, the regimes, or the budget; change the `none_indexed`/`not_checked` caveats. Filter `pvtl` client-side after an unfiltered query — the filter goes in the query so `count` stays honest. Guess a form for appendix rules.

**Unit tests.** The `laws_indexed` caveat equals the pinned wording (a literal comparison, so a drift fails loudly); the query builder emits `lawtype:public` in every query; the App. form for a numbered appendix section; the fallback for a rule; existing WO-1 tests updated to the new string, not loosened.

**Verification artifacts** (real upstream, traced, as for WO-1): (1) `10 U.S.C. 7274` → `laws_indexed` listing PLAW-119publ60 only, query string shown with `lawtype:public`; (2) `42 U.S.C. 2210` → still publ74 only; (3) `18 U.S.C. App. 1201` → a real detector outcome on the App. form (expected `none_indexed`, O44d measured 0 all-time), `checked_citation: 18 U.S.C. App. 1201`; (4) an appendix section expected to fire — find one by probing `uscodecitation:"{t} U.S.C. App. {s}"` with the open-ended `publishdate` bound for a section that resolves to a single current-edition granule, and record which; (5) `28 U.S.C. App. Rule 9` disambiguated to one granule by ids → `not_checked`/`no_measured_citation_form` unchanged; (6) the exact query strings for all of the above; (7) the `laws_indexed` object from (2) showing the pinned caveat verbatim, and the tools/list description text.

## WO-3 — `granule_id` on `get_us_code_section` (R16), the disambiguation message, and the server-instructions text (contract in `40-tools.md`; basis O9, O24, O45c)

**Status:** CLOSED 2026-09-15 — built at d15ddde + bb73359, verified O46 (artifact review, black-box probe including both Rule 9s, and a direct re-measurement of the not-found split). The order's "summary 404 → not_found" was wrong — upstream answers 400 `invalid granuleId` for a bad granule and 404 only for a bad package — and the implementation's two-kind `not_found` is ratified into the contract (O46c). One wording nit logged (O46d). The order text below is kept as issued, for the record.

**Change 1 — optional `granule_id` (and `package_id`) on `get_us_code_section`.** Schema: both optional strings; `granule_id` alone is sufficient. Behavior is specified in full in `40-tools.md` ("By-id behavior"); the load-bearing points: no URL is constructed from the id — fetch `GET /packages/{package_id}/granules/{granule_id}/summary` (O9) and use its `txtLink` verbatim; derive `package_id` from the id's `USCODE-{year}-title{n}` prefix only when not supplied, and say so (`package_id_derived: true`); reject non-USCODE ids before any call; summary 404 → `not_found` with the id echoed, never conflated with upstream failure or 429; `year` ignored with a warning; everything downstream of the fetch (provenance, `currentthrough`, windowing, `find`, `structure`, banner, `possibly_superseded`) is the citation path's code, not a copy. Detector on the id path: on the supplied `citation` after the usual strips if given, else `not_checked` / `no_citation_for_detector` with the message telling the consumer to pass `citation` alongside.

**Change 2 — the `ambiguous` message.** Replace "re-request by ids or year" with wording that names the real path: re-request with `granule_id` from the candidate list (plus the same `citation` to keep the staleness check), or with `year` if the candidates differ by edition.

**Change 3 — the server `instructions` string.** Bring it to the contract in `40-tools.md` ("Server instructions"): the indicator runs on every call so the consumer need not repeat the recipe; per-state next moves; the recipe reframed as the widening step. The reference text there may be used verbatim.

**Do not.** Construct `txtLink` or any URL from the id. Accept `granule_id` on `search_us_code` or the PLAW tools (out of scope). Change any `possibly_superseded` wording other than adding the new `not_checked` reason.

**Unit tests.** By-id success with a mocked summary + `/htm` (provenance and `possibly_superseded` present); `package_id` derivation disclosed; explicit `package_id` honored and not derived; summary 404 → `not_found` with the id; summary 500 and 429 → the distinct failure outcomes; non-USCODE id rejected with no HTTP call; `year` + `granule_id` → warning present; detector on `citation` + `granule_id`; detector `no_citation_for_detector` without `citation`; the `ambiguous` message names `granule_id` and never says "by ids"; the instructions string contains the required content (a literal check on the pinned sentences is acceptable).

**Verification artifacts** (real upstream, traced): (1) `28 U.S.C. App. Rule 9` → `ambiguous` with two candidates, message showing the new wording; (2) `granule_id: USCODE-2024-title28-app-federalru-dup1-rule9` + `citation: "28 U.S.C. App. Rule 9"` → `success`, provenance complete, `package_id_derived: true`, text of the Civil-rules Rule 9 ("Pleading Special Matters"), `possibly_superseded` = `not_checked`/`no_measured_citation_form` (a rule); (3) `granule_id: USCODE-2024-title17-chap1-sec107` + `citation: "17 U.S.C. 107"` → `success` with `possibly_superseded` `none_indexed` on `17 U.S.C. 107`; (4) the same id without `citation` → `not_checked`/`no_citation_for_detector`; (5) a plausible-but-nonexistent id → `not_found` with the upstream status shown; (6) the served `instructions` string from `initialize`.

## WO-4 — default `max_chars` 100,000 → 20,000 on `get_us_code_section` and `get_public_law` (contract in `40-tools.md`, "No silent truncation"; measured basis O47b, disposition of F3)

**Status:** CLOSED 2026-09-16 — built at 3967e66, verified O48 (artifact review and black-box probe, including the by-id path and `find` past the window). The order text below is kept as issued, for the record.

**Change.** The default `max_chars` on both text tools becomes 20,000. Nothing else about windowing changes: explicit `max_chars` above 20,000 is honored as before (the cap, if any, is unchanged), `start_char`, the banner, `total_chars`, `next_start_char`, `find`, and `structure` are untouched. The tool descriptions and the server instructions state the new default and, in one clause, why: windows much larger than this are measured not to reach the model inline on the current driver, so the first call would be spent discovering that.

**Why 20,000.** O47b: on claude-code 2.1.236 the 100,082-char default window (208 KB envelope) was not delivered inline in either cell that received it — the driver counted it and delivered a ~3 KB stand-in — while a 20,081-char window (43 KB envelope) in the same cell was delivered inline. 20,000 is just under the largest window measured to arrive. The exact threshold between 20,081 and 100,082 is not measured and is not needed for this order.

**Do not.** Change the cap or the banner. Add a summary-plus-pointer shape (F1's parked option; not triggered — O47a measured 1×). Touch `documentation/`.

**Unit tests.** Default resolves to 20,000 on both tools when `max_chars` is absent; an explicit larger value is honored; the banner and `next_start_char` are correct at the new default on a payload larger than it; the descriptions name the default.

**Verification artifacts** (real upstream, traced): (1) `get_public_law` for Pub. L. 118-31 with no `max_chars` → 20,000 chars returned, `total_chars: 3,590,552`, banner and `next_start_char: 20000`; (2) `get_us_code_section` `42 U.S.C. 2210` with no `max_chars` → 20,000 of 116,126; (3) the same with `max_chars: 100000` → 100,000 returned; (4) the served descriptions from tools/list.

## WO-5 — description and caveat fixes from the full run (F4, F5, F6; contract in `40-tools.md`; measured basis O49, run 2026-09-16T012953Z)

**Status:** CLOSED 2026-09-17 — built at 4f238a6, verified O50 (artifact review and black-box probe of all three descriptions and six query shapes, including the `packageid:` recipe live and an `approveddate` failure still carrying no caveat). F4's consumer-side effect is measured on the next run's D2/isolation. The order text below is kept as issued, for the record.

**Change 1 — `search_public_laws` description: drop `approveddate`, name `publishdate`.** The served description lists `approveddate:range(...)` as a useful field; ranges on it return HTTP 500 upstream (O43f) and two of three A4 cells followed the description into that error (O49a). Replace with `publishdate:range(YYYY-MM-DD,)` (open-ended form measured, O43f) and say in one clause that `approveddate` ranges fail upstream.

**Change 2 — both search descriptions: the `packageid:` scoping recipe.** Required by the contract since E13 (O30), measured absent from both served descriptions (F6b). USCODE: `packageid:USCODE-2024-title17 <terms>` for within-title section-level search; PLAW: `packageid:PLAW-118publ31 <terms>` as a within-law presence test. One sentence each, with the caveat that it tests presence, not location — `find` on the text tool locates.

**Change 3 — `get_public_law` description: the numbered path is the public series.** Add: `congress` + `law_number` names the PUBLIC-law series only; do not use it for a private-law number — pass the citation string ("Private Law 118-1") and the server returns the out-of-scope outcome. Basis F4.

**Change 4 — `recall_caveat` on every `search_public_laws` success.** Today it rides only on queries containing `uscodecitation:` (5 of 5 carried it, 0 of 6 others did). Emit it on every success: the existing measured-gap text when the query contains `uscodecitation:`; otherwise a caveat that full-text matching has unmeasured gaps — a quoted phrase was measured missing a law that contains it verbatim (O49c) — and that absence from results is not evidence of absence. The check `recall-caveat-always` is unchanged and will pass once this lands.

**Do not.** Change the search tools' arguments or result shapes beyond the new caveat text. Touch `documentation/`.

**Unit tests.** Descriptions: contain `publishdate`, do not contain `approveddate:range`, contain `packageid:` (both search tools), and the get_public_law public-series sentence. Caveat: present on a mocked success for a `uscodecitation:` query, a full-text query, and a `congress:`/`docnumber:` query, with the two wordings selected correctly; absent on `upstream_error`/`rate_limited` (unchanged).

**Verification artifacts** (real upstream, traced): (1) tools/list descriptions for all three tools; (2) `search_public_laws` with `congress:119 "Price-Anderson"` → success, `count: 0`, `recall_caveat` present with the full-text wording; (3) `uscodecitation:"42 U.S.C. 2210"` → `recall_caveat` with the field wording; (4) the two query strings and both caveat texts verbatim.

## WO-6 — the audience sentence in `text.message` on truncated responses (contract in `40-tools.md`, "No silent truncation"; measured basis O51; consumer effect preregistered as E17)

**Status:** CLOSED 2026-09-19 — built at implementation commit ee4ca88, verified O52: the run's four trace lines checked programmatically (banner shape and position, continuation sentence byte-unchanged and first, audience sentence after one space with the URL equal to the response's own `provenance.pdf_link`, no "provenance" in the message, nothing on un-truncated responses), a fresh-process probe over five inputs including the continuation window and the default-window `17 U.S.C. 107` truncation, and a clean secret scan. Final wording from the trace: `The start_char continuation is for the tool caller, not the person asking. A person who wants the whole document should be given the PDF link: <pdf_link>`. The no-`pdfLink` wording is unobserved upstream and stands as the implementation's claim (O52d). The order text below is kept as issued.

**What it is.** When `truncated` is true on either text tool (`get_public_law`, `get_us_code_section`), `text.message` gains a sentence saying that the `start_char` continuation is for the tool caller, and that a person who wants the whole document should be given the PDF link — with the URL from the same response's `provenance` written into the sentence. The banner is unchanged: its exact text is pinned by the `banner-leads-truncated-content` check and O37. Non-truncated responses are unchanged.

**Why.** O51c, from the harness's loop-driver runs of C1: 21 of 21 answers relayed the continuation verbatim to an asker who has no tool access; 0 of 10 pinned answers gave the PDF link the response carried; one told the asker the link was "in the provenance metadata". Consumers repeat `text.message` — F1 closed the same way when the truncation truth moved in-band (E12).

**Shape.** Exact wording is the implementation's; it must: name the continuation as the tool caller's; name the PDF as the whole-document route for the person asking; carry the URL inline, never a pointer to the `provenance` field; be one or two sentences; appear on both text tools; follow the existing continuation sentence so R13c's banner-outside-the-coordinates statement is kept intact. Report the final wording verbatim.

**Do not.** Change the banner text or position. Add the sentence to non-truncated responses. Change any field name or the shape of the `text` object. Write under `documentation/`.

**Unit tests.** On truncated `get_public_law` and `get_us_code_section` responses: the message contains the URL from that response's provenance, names the PDF, and the existing continuation sentence and the banner are byte-unchanged from before. On non-truncated responses: no audience sentence. A provenance with no PDF link, if that case is reachable: the sentence says the link is unavailable rather than emitting an empty URL — and if the case is not reachable, say so in the report instead of testing a case that cannot occur.

**Verification artifacts** (real upstream, traced): (1) `get_public_law` on `Public Law 118-31` at the default `max_chars` — the trace record with `text.message` verbatim, URL inline; (2) `get_us_code_section` on `42 U.S.C. 2210` at `max_chars` 2000 — the same; (3) one un-truncated response from each tool showing no audience sentence; (4) the final wording, verbatim, in the report. Whether consumers repeat it to the asker is E17's measurement, not this order's.

## WO-7 — the public PDF link: `public_pdf_link` and `details_link` in provenance, and the audience sentence carries the public link (contract in `40-tools.md`, "Provenance on every text payload" and "No silent truncation"; measured basis O53)

**Status:** CLOSED 2026-09-19 — built at implementation commit c373a2e, verified O54: the run's four trace lines checked programmatically (public link built from each record's own ids, `pdf_link` unchanged, banner and continuation byte-unchanged, audience sentence carrying `public_pdf_link` and never `pdf_link`, both fields and no sentence on un-truncated responses); every served `public_pdf_link` fetched by the spec session without a key — 7 of 7 HTTP 200 `application/pdf`, the run's four plus three from a fresh-process probe covering the continuation window, a 2015 edition, and a 2014 appendix granule; the citation-path `details_link: null` reproduced against upstream and accepted with the rationale in `40-tools.md`. The `details_link` fallback wording is unreachable by construction and the no-link wording stays unobserved upstream (O54f). The order text below is kept as issued.

**What it is.** WO-6's sentence hands the asker `download.pdfLink`, which lives on `api.govinfo.gov` and returns 401 without the key (O53a) — the person it is addressed to cannot open it. Every text-bearing success on both tools, truncated or not, gains two provenance fields: `public_pdf_link` — `https://www.govinfo.gov/content/pkg/{package_id}/pdf/{package_id}.pdf` for a package (`get_public_law`), `https://www.govinfo.gov/content/pkg/{package_id}/pdf/{granule_id}.pdf` for a granule (`get_us_code_section`), built from the identifiers the response already carries and from nothing else — and `details_link`, the upstream summary's `detailsLink` verbatim. `pdf_link` stays exactly as it is (two Layer 1 checks pin it). The audience sentence's URL becomes `public_pdf_link`; its wording is otherwise unchanged. If `public_pdf_link` cannot be built, the sentence gives `details_link`; if neither exists, the existing no-link wording. State in the report whether either fallback is reachable.

**Why the content path and not the Link Service.** GovInfo's Link Service (S14) resolves by citation form and lands on the content path (O53c). The content path is built from the ids the server resolved by, so it names the same granule the text came from — including `granule_id` lookups (R16) and appendix rules, where a citation-form resolver could land elsewhere. Measured 4 of 4 directly and 7 of 7 through the service (O53).

**Do not.** Fetch or HEAD the public link to verify it — no extra upstream call. Change `pdf_link`. Change the banner, the continuation sentence, or the audience wording beyond the URL. Write under `documentation/`.

**Unit tests.** Both tools: `public_pdf_link` matches the pattern with that response's own `package_id` (and `granule_id`), `details_link` equals the mocked summary's `detailsLink`, the audience sentence on a truncated window carries `public_pdf_link` and does not carry `pdf_link`; un-truncated responses carry both fields and no sentence; a summary without `detailsLink` yields `details_link: null`, not an error; each fallback wording.

**Verification artifacts** (real upstream, traced): (1) `get_public_law` `Public Law 118-31` at the default window — provenance with all three links, the sentence carrying `https://www.govinfo.gov/content/pkg/PLAW-118publ31/pdf/PLAW-118publ31.pdf`; (2) `get_us_code_section` `42 U.S.C. 2210` at `max_chars` 2000 — the granule form; (3) `get_us_code_section` by `granule_id` (the Rule 9 case, O46) — `public_pdf_link` names that granule; (4) one un-truncated response with both fields and no sentence; (5) every served `public_pdf_link` verbatim. This session will fetch each `public_pdf_link` from the traces without a key and record the status.

## WO-8 — collection scoping on the two search tools, and the README warning for the HTTP transport (R17; contract in `40-tools.md`, "Collection scoping"; measured basis O55)

**Status:** CLOSED 2026-09-20 — built at implementation commit a4e4e04, verified O57 as conforming to this order: the O55a calls replayed by the spec session with none of the foreign ids returned, the refusal shape as specified, refusals measured making no upstream request (a DNS-down run in the report's own artifacts, O57c), the detector-query paste working, the README warning in place. **The order itself was insufficient**: its quoted-phrase exemption lets an unbalanced quote carry a foreign clause upstream (O57e), and the build reads a clause's value short of the whole token (O57f). Both go to WO-9. The order text below is kept as issued, including the exemption now withdrawn.

**What it is.** Two things, one ruling. (1) `search_us_code` and `search_public_laws` each validate the collection clauses in `query` and refuse a non-conforming one before any upstream request. Today either tool runs whatever collection the caller names: `search_us_code` returned `PLAW-118publ31` for `collection:PLAW congress:118 docnumber:31` and 22,940 `BILLS` hits for `collection:BILLS congress:118`; `search_public_laws` returned `USCODE-2024-title17` with the public-law `recall_caveat` attached (O55a). That was the spec's defect — the old text said "unless the query already contains a `collection:` term" — and the contract is corrected. (2) A warning in the README that the HTTP transport is unauthenticated: anyone who can reach the port can call every tool and spend the GovInfo key's quota, so bind it to loopback unless something in front of it authenticates. The maintainer ruled that the transport itself does not change (R17b).

**The rule, restated so this order stands alone.** Find every collection clause in `query`: the word `collection` in any letter case, optional whitespace, a colon — anywhere outside a double-quoted phrase, inside parenthesized groups included. A clause conforms only if its value is the tool's own collection as one bare or double-quoted token, any letter case, no whitespace after the colon. All clauses conform → send the caller's query as written, prepending the tool's canonical clause only when the query has no clause (existing behavior, byte-unchanged for every query that works correctly today). Any clause does not conform → `outcome: out_of_scope_collection`, no upstream request, carrying the offending clause verbatim, the tool's collection, the name of the tool that serves the named collection when there is one, and a statement that no search was run.

**Why refuse instead of overriding.** Upstream unions repeated collection terms: `collection:USCODE collection:PLAW "fair use"` returns 18 = 11 + 7, and so does the foreign clause nested in parentheses (O55c). Prepending the right clause beside a wrong one scopes nothing. And `collection: USCODE` with a space is not read by upstream as a restriction at all (O55d) — under a substring presence test it suppresses the prepend and unscopes the tool entirely, so it is refused, not passed.

**Do not.** Parenthesize or otherwise rewrite the caller's query — a top-level OR was measured not to escape the prepended clause (O55e). Strip or repair a non-conforming clause. Filter foreign hits out of a result after the fact (the `count` would then lie). Apply the rule to the queries the server builds itself (citation resolution, the `possibly_superseded` detector). Add authentication, a loopback-only restriction, or any other change to the HTTP transport. Act on the audit's other findings (key egress, trace permissions, dependency auditing) — they are with the maintainer as Q15. Write under `documentation/`.

**Unit tests.** Per tool, asserting on the request the mocked upstream did or did not receive: no clause → canonical clause prepended; conforming clause in each measured spelling (`collection:USCODE`, lowercase value, uppercase field name, quoted value) → query sent as written, not double-prepended; the detector's echoed query shape (`collection:PLAW lawtype:public publishdate:range(…) uscodecitation:"…"`) accepted by `search_public_laws` unchanged. Refusals, each asserting zero upstream requests and the `out_of_scope_collection` shape: the other tool's collection (with the pointer to that tool), a third collection (`BILLS`, no pointer), `collection:(USCODE OR PLAW)`, a conforming clause plus a foreign one, a foreign clause nested in parentheses, the space-after-colon form, a negated clause, an empty value, mixed-case foreign (`Collection:plaw` on `search_us_code`). Not a refusal: `collection:` inside a double-quoted phrase. The refusal is traced like any other response (R8). A note on these tests, from this directory's standing rules: they and the validator will be written from the same reading of this order, so they prove the two agree with each other, not with GovInfo — the inputs below are the ones no one here authored.

**Verification artifacts** (real upstream, R8 trace on, under the gitignored `runs/`): replay the five served calls of O55a — same tools, same arguments, in `runs/2026-09-20-e18-spec-probe/probe.py` — and return the trace: the two controls unchanged (`collection:USCODE "fair use"` echoed, 11; PLAW 118/31, 1), the three foreign-collection calls refused with the shape above. Add: `search_us_code` with `collection: USCODE "fair use"` (refused), with `collection:uscode "fair use"` (success, 11 at time of writing, query echoed as sent), and `search_public_laws` with a `query` pasted verbatim from a live `possibly_superseded` object (success). For the refusals, the evidence that no upstream request was made — whatever the server can honestly show (request log, timing); say which. For the README: the diff. This session will re-run its own probe against the built server and compare sets, not counts.

## WO-9 — collection scoping, corrected: no quoted-phrase exemption, whole-token values (R17; contract in `40-tools.md`, "Collection scoping"; measured basis O57e/O57f)

**Status:** CLOSED 2026-09-20 — built at becc5f2, verified O59: the 52-call replay diffed trace against trace, exactly the 15 predicted rows now refused (the three bypasses among them), 37 unchanged, the report's replay agreeing on all 52; 15 further inputs from no order found no bypass. The order text below is kept as issued.

**What it is.** WO-8 was built exactly to its order and the order was wrong. `search_us_code` with `"fair use collection:PLAW` — a single unbalanced double quote — returns 18 hits, 11 USCODE and 7 PLAW; `"fair \" collection:PLAW "fair use"` does the same; `search_public_laws` with `"appropriations collection:USCODE` returns 7,552 hits, 100 of 100 on the page USCODE, under the public-law `recall_caveat` (O57e). WO-8 told the validator to skip clauses inside a double-quoted phrase; upstream does not treat an unterminated or escaped quote as a phrase and honors the clause. Two changes. **(1) No exemptions.** A collection clause is the letters `collection` (any case), optional whitespace, a colon — anywhere in the string. Quotes do not hide it, parentheses do not hide it, preceding characters do not hide it (`subcollection:PLAW` is a clause). Remove the quote handling; do not replace it with a better one — any quote parser the server runs is one that can disagree with upstream's. **(2) Whole-token values.** The value runs from the colon to the next whitespace, closing parenthesis, or end of query; when it opens with a double quote, through the next double quote, which must be followed by whitespace, `)`, or the end. It conforms only if the whole value equals the tool's collection, bare or double-quoted, any case. Today `collection:USCODE*`, `USCODE,PLAW`, `USCODE~`, `USCODE^2`, `USCODE/PLAW`, `USCODE|PLAW`, `USCODE.PLAW`, `USCODE:PLAW` and `USCODE"PLAW"` pass validation and reach upstream, where each returns HTTP 500 (O57f) — no foreign hit measured, but they are requests the contract says are never sent.

**Refusal message.** When the offending clause sits inside double quotes, the `message` adds that a phrase containing `collection:` must be written without the colon. Otherwise the `out_of_scope_collection` shape is unchanged.

**Do not.** Change anything O57 verified: the refusal shape, the prepend-only-when-absent rule, the echo of the caller's query as sent, conforming spellings, the README. Add operator analysis for conforming clauses — `AND NOT collection:USCODE`, `OR collection:USCODE` and `NOT collection:USCODE` were measured staying in-collection (O57g). Write under `documentation/`.

**Unit tests.** Refused, zero upstream requests each: the three O57e queries verbatim; a foreign clause inside a *balanced* quoted phrase (`"x collection:PLAW y"`); `"the collection: of duties"` with the colon advice in the message; `subcollection:PLAW`; each O57f value form. Still accepted, sent as written: every conforming spelling from WO-8, `collection:USCODE)` closing a group as in `(collection:USCODE "fair use")`, a conforming quoted value followed by `)`. The WO-8 tests that assert the quoted-phrase exemption are inverted, not deleted.

**Verification artifacts** (real upstream, R8 trace on, under `runs/`): replay `runs/2026-09-20-wo8-spec-probe/probe.py`, `probe2.py` and `probe3.py` — 52 calls — and return the traces. Expected against O57: the three O57e bypasses, the two quoted-phrase successes of O57d, the O57f forms and `subcollection:` become refusals; every other row unchanged in outcome, count and prefix set. This session will re-run the same scripts and diff the rows. (Afterword, O59f: those scripts write summaries to fixed names in their own directory, and the replay overwrote this session's. Later orders name a fresh output directory for any replay.)

## WO-10 — the audit's remaining findings: key bound to the API origin, trace files `0600`, dependency audit in GitHub Actions (R18; contracts in `40-tools.md`, "Secret hygiene" and "Trace emission"; measured basis O56, O58)

**Status:** CLOSED 2026-09-20, all three parts — part C's first Actions run is in: 4 of 4 jobs `success`, non-vacuity checked (O60). History of the closure: parts A and B closed first, part C built with its artifact owed (O59). A (440fb8e): retrieval regression identical to the 2026-09-19 trace but for timings; the refusals themselves are unobservable against real upstream and rest on unit tests this session does not read — claimed, not measured, as this order anticipated; the API-base question is answered by the maintainer (S16): no setting exists, only an unexposed constructor argument that predates this order. B (8d55b75): measured — five server-created trace files `0600` under `umask 000`. C (418d644): the workflow is as ordered, but the repository has no git remote, so it has never run; the first-run URL and conclusion are owed once a GitHub remote exists. Issued 2026-09-20; independent of WO-9; three parts, three commits.

**Part A — the key goes to one origin.** Attach `X-Api-Key` only when the request URL is scheme `https`, host exactly `api.govinfo.gov`, default port, no userinfo. Anything else is not fetched — not keyed, not keyless — and surfaces as `upstream_error` with `http_status: null` and a `detail` carrying the refused URL verbatim, the upstream field it came from, and that no request was made; inside `possibly_superseded`, `not_checked` / `malformed_response`. Keyed requests never follow redirects; a 3xx is `upstream_error` with the status and `Location` verbatim. Enforce it at the single place requests leave the process, so no caller can forget it. If a setting moves the API base for tests, the allowed origin is that configured origin, `http` only for a loopback address — name the setting in the report; the spec does not know of one. Nothing that works today changes: 3,033 of 3,033 observed links are on the origin (O56), 15 of 15 link kinds answer 200 without redirecting (O58). *Unit tests*, each asserting that no request reached the foreign host and that the key appears in no request other than to the allowed origin: a search hit or summary whose `txtLink` is `http://api.govinfo.gov/…`, `https://api.govinfo.gov.example.com/…`, `https://x.api.govinfo.gov/…`, `https://api.govinfo.gov:8443/…`, `https://api.govinfo.gov@example.com/…`, `https://example.com/…`, `file:///etc/passwd`; a mocked 302 from an allowed URL to an off-origin `Location` and to an on-origin one — neither followed; the `granule_id` path (R16) where the link comes from a summary; the `uslm` format path on `get_public_law`. The unchanged happy paths stay green. *Artifacts:* this rule cannot be exercised against real upstream, so the evidence is the tests plus a real-upstream regression trace — the WO-7 artifact calls (1)–(4) replayed, outcomes and provenance unchanged. Say plainly in the report that the refusals are mock-only.

**Part B — trace files `0600`.** Every file the server creates in `USCODE_MCP_TRACE_DIR` is created mode `0600` regardless of umask. Do not chmod the directory, do not touch files the server did not create, no symlink or collision handling. *Unit tests:* under umask `022` and under `000`, the created file's mode is `0600`; appends after creation leave it so; the existing R8 behaviors (startup failure on an unusable directory, loud per-request write failure) unchanged. *Artifacts:* `stat` output for a trace file from a real server process started under `umask 000`. This session will repeat that with its own probe.

**Part C — dependency audit in GitHub Actions.** The maintainer's placement: the GitHub Actions workflow. No `.github/workflows/` directory exists at the time of writing, so this creates it. `pip-audit` over the locked runtime and development dependency sets (from `uv.lock`, hashes respected), once against the PyPI advisory service and once against OSV, on push and pull request to `main` and on a weekly schedule; a known vulnerability fails the job. No dependency changes — S15 reports none needed, and any the audit does flag come back as a finding, not as a silent bump (R9: ceilings move deliberately). Whether the same workflow should also run the test suite and `ruff` was not asked and is not ordered; mention it in the report if you think it should. *Artifacts:* the workflow file path and the URL and conclusion of its first run on GitHub.

**Do not.** Add authentication or bind restrictions to the HTTP transport (R17b). Touch collection scoping (WO-9). Write under `documentation/`.

## WO-11 — the code review's findings: public-law resolution, the by-id citation, window validation, caveat case, wrongly-typed bodies (S17; contracts in `40-tools.md` and `30-search.md`; measured basis O61)

**Status:** Parts A–D CLOSED 2026-09-20 — built at ab6ef01, f4d8f49, d239916, 8ee649e, verified O62 (artifact review and a 91-call black-box probe: 60 of 60 public laws now resolve, against 0 of 60 at O61b). Parts E (ad159ed) and F (8f9b7bd) CLOSED as built-and-claimed: unit-level by nature, as ordered; this session cannot observe either and says so (O62c). Part G (b9d9185) CLOSED 2026-09-21: first run `success`, 500 tests on CPython 3.11.16, Ruff clean (O63); it had been built with its run owed until the commits were pushed (O62d). All seven parts closed. Issued 2026-09-20, amended the same day before it was handed to the implementation session (R19: parts F and G added, finding 6 moved from noted to ordered). The order text below is kept as issued. Seven parts, independent; one commit each is preferred so a verification failure names its part. Part A is the urgent one: it is a served defect reaching 60 public laws today.

**Part A — `get_public_law` resolves with `lawtype:public` and confirms the package by identity.** The resolution query becomes exactly `collection:PLAW lawtype:public congress:{n} docnumber:{m}`, on both the numbered path and the citation path. The single hit's `packageId` must equal `PLAW-{n}publ{m}`; one hit with any other id is `upstream_error` naming the id received. Why: `docnumber` is shared between the public and private series — for all 60 private laws that exist, the bare query returns both packages, the private one first in 34 (O61a) — and the served server answers `ambiguous` for every one of those 60 public laws, with a message telling the caller to re-request by the congress and number it just used (O61b). The code review (S17) described this as "returns a private law"; against real upstream it does not, because a private law never comes back alone — that was its fixture, not GovInfo. The fix is the one it recommended all the same. The spec was wrong first: it pinned the query without `lawtype`. Also change the `ambiguous` message so it never points back at the same inputs: the number did not resolve to one public law; the candidates' `package_id`s are listed as findings. `out_of_scope_private_law` is unchanged. *Unit tests:* the query string, literally, on both paths; one `publ` hit → success; one hit whose id is `PLAW-119pvtl2` → `upstream_error`, not success (use the real id form, `pvtl` — `priv` is not one); zero hits → `not_found` unchanged; two hits → `ambiguous` with the new message; 500 and 429 unchanged. *Artifacts:* `get_public_law` by `congress` + `law_number` for all 60 pairs in `runs/2026-09-20-e20-spec-probe/00-upstream.json` (`arm1[].congress`, `.n`), `max_chars` small, traced: expected 60 of 60 `success` with `provenance.package_id` equal to `PLAW-{c}publ{n}`; plus `Pub. L. 119-2` by citation, `Private Law 119-2`, and the 118/31 control.

**Part B — the by-id citation is the caller's word.** On `get_us_code_section` with `granule_id` and `citation`: (1) before any upstream request, compare the citation's title with the `title{n}` segment of the package id (supplied or derived); a mismatch is `invalid_argument` naming both titles and stating nothing was fetched. (2) On every id-path success with a citation, `normalization.citation_basis` is `caller_supplied`, and `possibly_superseded` carries `citation_statement`: the check ran on the citation the caller supplied, which the server did not verify names this granule; if it does not, this result is about a different section. The citation path carries `citation_basis: resolved` and no statement. Both fields are additive; no existing field changes. Basis O61d. Appendix ids: the title comparison uses the package's title the same way (`USCODE-2024-title28` for `28 U.S.C. App. …`). *Unit tests:* title mismatch → `invalid_argument`, zero upstream requests; explicit `package_id` whose title disagrees with the citation → the same; matching title → success with `caller_supplied` and the statement in all three detector states; citation path → `resolved`, no statement; id path without `citation` → `no_citation_for_detector` unchanged, no `citation_basis` claim to make. *Artifacts, traced:* the O61d pair (title 17 granule with `42 U.S.C. 2210` → `invalid_argument`; with `17 U.S.C. 107` → success showing both new fields), the same granule with `17 U.S.C. 106` (same title, wrong section → success, statement present — the case the statement exists for), and the O46 Rule 9 by-id call.

**Part C — window arguments.** `max_chars` ≤ 0 and `start_char` < 0 are refused before any upstream request on both text tools, by-id path included. A `start_char` > 0 and ≥ `total_chars` is `invalid_argument` carrying `total_chars`, saying no text exists at that offset and that the document was retrieved and is not empty. `start_char: 0` on an empty payload stays `success`. Basis O61e. *Unit tests:* each pre-request refusal asserts zero upstream requests, on every path of both tools; past-the-end on both tools, at `start_char == total_chars` and beyond; the last legitimate window (`start_char = total_chars − 1`) still succeeds; `find` and `structure` behavior unchanged. *Artifacts, traced:* replay arm 5 of `runs/2026-09-20-e20-spec-probe/served.py` into a fresh directory, with per-call wall-clock: the argument refusals should sit near the local-reject control (6 ms at O61e), not near a lookup (537–1,166 ms).

**Part D — `recall_caveat` selection ignores letter case in the field name.** `USCODECITATION:`, `UsCodeCitation:` and the lowercase form all select the measured-gap wording (O61c: upstream reads them as one field). The query is still sent upstream exactly as the caller wrote it. *Unit tests:* the three spellings; the phrase `"42 U.S.C. 2210"` without the field → full-text wording; existing caveat tests unchanged. *Artifacts:* arm 3 of the same script replayed.

**Part E — a wrongly-typed 2xx body is `upstream_error`, not an exception.** At the place response bodies are parsed: not JSON, `results` not a list of objects, a package or granule summary that is not an object, a required field of the wrong type → `upstream_error` with the HTTP status, the body verbatim, and a `detail` naming what was expected and what arrived; in the detector, `not_checked` / `malformed_response` as today. This part rests on S17's claim alone — this session cannot see it from outside and no such body is in any run artifact (O61f) — so its verification is unit-level and the report should say so. Keep it to shape validation where bodies enter; do not thread type checks through every field access. *Unit tests:* S17's two shapes (string-valued `results`, list-valued summary) on every tool that reads them, plus a `results` list containing a non-object; each asserts the structured outcome and that the request is traced (R8) like any other.

**Part F — a server-created HTTP client is closed at shutdown (R19b; contract in `40-tools.md`, "Resource lifecycle").** S17 finding 6: the server lazily creates its GovInfo client and installs no lifespan that closes it. Install one: when the server shuts down — stdio and HTTP transports both — a client the server itself created is closed; a client injected by the caller is never closed by the server, and a server that never created one (no tool was called) closes nothing and does not create one in order to close it. The maintainer ruled this a correctness defect; an earlier version of this order filed it as "noted, not ordered" because it is invisible from outside the process, and that was this session's error (R19). *Unit tests:* lifespan exit closes a server-created client exactly once; an injected client is left open; shutdown before any tool call creates and closes nothing; a close that raises does not mask the shutdown. *Artifacts:* none from real upstream are possible; say in the report that verification is unit-level. If the suite can run with `-W error::ResourceWarning` clean, say so — that is the nearest thing to an outside view.

**Part G — CI runs the unit suite and Ruff (R19a).** A GitHub Actions workflow separate from the dependency audit, so an advisory-service outage cannot mask a test failure or the reverse: on push and pull request to `main`, install from `uv.lock`, run the full unit suite and `ruff check .`; either failing fails the job. Offline — the suite mocks upstream; the workflow holds no GovInfo key and no secret of any kind. Python version: the one the project pins. No spec contract stands behind this; it is repository tooling, like WO-10 part C. *Artifacts:* the workflow path, and the URL and conclusion of its first run; plus non-vacuity — the test count from the run's log (451 at S17's reading; the number will have grown with this order) so a green run of zero tests cannot pass for one.

**Do not.** Filter private packages out of a result after an unfiltered query — the filter goes in the query (the WO-2 rule). Add a `package_id` input to `get_public_law`. Parse granule ids beyond the leading `USCODE-{year}-title{n}` segments. Run the citation search on the id path to verify the section (considered and declined, `40-tools.md`). Change the banner, `text.message` wording, or any pinned caveat. Write under `documentation/`. Write replay output into `runs/2026-09-20-e20-spec-probe/` — use a fresh directory (O59f).

**A note on the tests, as on WO-8's:** parts B, C and E will be tested by fixtures written from the same reading of this order as the fix. They prove the two agree. Parts A and D are the ones real upstream can contradict, which is why their artifacts are replays of inputs GovInfo authored — the 60 pairs — and not examples chosen here.

## WO-12 — served strings: no spec codes, no source indentation, one window convention, the description teaches the small locating call, and the E22 banner-carrier switch (S18, S19; contract in `40-tools.md`, "Served strings are written for the consumer", "No silent truncation", "Locating content in large payloads"; measured basis O64)

**Status:** CLOSED 2026-09-21 — built at `ea84893` (parts A–E) and `8c3e481` (part F), verified O66 by an independent fresh-process probe: zero spec identifiers across the surface and 34 responses in every outcome and state, zero double spaces, one window convention, the locating call taught, surface 12,518 → 12,048, and the three banner modes differing at exactly the three specified places over byte-identical payloads, with invalid values failing startup. History: DRAFTED 2026-09-20, not yet issued; amended in place the same day (S19, O65): part C withdrawn. Amended again 2026-09-21 (R21): part F added. Five live parts — A, B, D, E, F — no upstream behavior involved. One commit for A–E is fine; part F in its own commit, because it is removed again when E22 closes. Q17 is ruled (R20) and changes nothing here. The E22 run waits on all five: its arms must differ by the carrier and nothing else, so the wording fixes land first.

**Part A — remove spec-internal identifiers from every served string.** Measured at `b9d9185` (O64b), exactly these: `(O43b)` and `(O21)` in the `none_indexed` `possibly_superseded.caveat`; `(O36)` in `structure.note`; `O21, O17/O18` in the `search_public_laws` description. Delete the identifier and its parentheses or dash-clause and nothing else — the figures and the sentences stay. The `none_indexed` caveat was copied from WO-1's table, where this session wrote its citations inside the sentence; that is this spec's error and the wording as it should be served is:

> THIS IS NOT A CURRENCY CHECK. No public law published after this edition's currentthrough date is indexed against this section in GovInfo's uscodecitation field — but absence of a match is not evidence the text is current: the index misses about one in seven listed (law, section) pairs and one in four in an earlier sample. A law that amended this section may exist and simply not be indexed against it.

Then add a unit test that fails if any served string — `instructions`, every tool description and schema description, and every string in a representative response of each outcome and each `possibly_superseded` state — matches `\b[OREFSQ]\d{1,3}[a-z]?\b` or `WO-\d+`. The probe found no others, but it exercised one state of one tool; the test is what covers `laws_indexed`, `not_checked`, the refusals and the error outcomes.

**Part B — descriptions served without source indentation.** Every continuation line of all four tool descriptions is served with eight or more leading spaces, 760 characters in all (O64a). Serve the paragraphs and list items with their line breaks and no layout indentation. Test: no served description contains a run of two or more spaces.

**Part C — WITHDRAWN before issue.** It ordered `text.banner` dropped as the unspecified copy. The maintainer and the consumer asked why that one and not the line in `text.content`, and the traces show no run has ever served one without the other (O65), so there is no evidence for dropping either. Leave both carriers exactly as served. The letter is kept so parts D and E keep their names.

**Part D — `text.message` states the window the way the banner does.** Served: "returned chars 0-6000" beside a banner reading "chars 0–5,999". The banner is pinned character-exact (O37), so the message takes the banner's inclusive bounds. The rest of the message — the continuation, the coordinate disclosure, the audience sentence with `public_pdf_link` — is unchanged.

**Part E — the `get_us_code_section` description teaches the small locating call.** Content required, wording yours, two sentences at most, in the "DON'T GUESS OFFSETS" block: that the statutory text is the first field after a short header, so a `max_chars` of about 6,000 usually returns the statute and its source credit whole along with `structure`; and that when a section's size is unknown a very small `max_chars` returns `structure` alone for a few thousand characters of envelope. Parts A and B remove more characters than this adds; report the served surface total before and after (12,518 at `b9d9185`, measured as `instructions` + each description + each input schema as compact JSON).

**Part F — the experimental switch `USCODE_MCP_BANNER_CARRIERS` (contract in `40-tools.md`, "Experimental switch"; R21).** Values `both` (also the unset behavior), `field`, `content`; anything else, blank included, fails startup naming the variable. `field` serves `text.banner` and no banner line in `text.content`, and drops the R13c coordinate sentence from `text.message`; `content` serves the banner line and no `text.banner`, R13c sentence kept. Everything else byte-identical across modes for the same request — that identity is the thing to test: for one truncated `get_public_law` and one truncated `get_us_code_section`, the three modes' responses must differ at exactly `text.content`'s first line, the presence of `text.banner`, and the one sentence of `text.message`. Unit tests for each mode, for the startup failure, and for unset equalling `both`. Under `field` the manifest's `banner-leads-truncated-content` check fails by construction; that is the arm, not a defect, and no check is changed for it. Artifacts: the R8 trace of the same two truncated calls under each of the three modes.

**Verification artifacts, under the gitignored `runs/`:** the R8 trace of `get_us_code_section` on `17 U.S.C. 107` at `max_chars` 6,000, 1, and a reading call, and of one truncated `get_public_law`; the `tools/list` result and `instructions` as served; the before/after surface totals. This session will re-run E21's arms 1–3 against the build.

## WO-13 — the banner leaves `text.content`; the switch goes (E22 outcome, O76; contract in `40-tools.md`, "No silent truncation" and "Locating content in large payloads")

**Status:** CLOSED 2026-09-23 — built at `0436218` (one commit, parts A–D), verified O79 by an independent fresh-process probe: payload-only `text.content` on every truncated call with `text.banner` beside it and `returned_chars` equal to the content length, byte-equal to O66's `field` mode; the R13c sentence gone with the continuation and audience sentences unchanged; the variable ignored at every former value, valid or not; the ≤ 0 refusal teaching `max_chars: 1` on both tools with no upstream request. One unordered change accepted: the `get_public_law` description's banner clause now describes the new shape (O79e). Manifest version 7 follows (`60-e2e-harness.md`). History: ISSUED 2026-09-21 as three parts, one commit; part D added 2026-09-22 before pick-up; E25 ran before it landed, as allowed.

**Part A — `text.content` is the payload only.** On every truncated response of both text tools, `text.content` begins at the window's first character; the banner line is not prepended. `text.banner` (the banner line verbatim, present exactly when `truncated` is true) and `text.message` are unchanged except as part B says. `returned_chars` equals `len(text.content)` — it already does under the `field` mode (O66f), and this is that mode made the only one.

**Part B — the R13c sentence is retired.** `text.message` no longer says the banner leads `content` and offsets start after it; there is no banner in `content` to disclaim. The continuation sentence and the audience sentence stay word for word.

**Part C — remove `USCODE_MCP_BANNER_CARRIERS`** and its startup validation and tests; the variable, if set, is ignored like any unknown variable. Update the unit tests that asserted the three-mode identity (WO-12 part F) to assert the single shape.

**Part D — the `max_chars` ≤ 0 refusal teaches `max_chars: 1`** (contract in `40-tools.md`, "No silent truncation"; added 2026-09-22 before the order was picked up). Both text tools: the `invalid_argument` `detail` for `max_chars` ≤ 0 says that `max_chars` must be at least 1 and that a locating call (`structure`, `find`) should pass `max_chars: 1`. Wording yours; unit test on both tools. Measured need: two consumers asked `find` with `max_chars: 0` (O75; run 20260922T120550Z).

**Verification artifacts, under the gitignored `runs/`:** the R8 trace of one truncated `get_us_code_section` and one truncated `get_public_law`, a reading call, one untruncated call, and one `max_chars: 0` refusal; the served `tools/list`. This session verifies by the O66 probe with the mode arms deleted, then republishes the manifest as version 7 with `banner-leads-truncated-content` reading `/response/structuredContent/text/banner`.

## WO-14 — the `laws_indexed` caveat names its reader (R23; contract in `40-tools.md`, "Staleness indicator"; measured basis F9/O67b; consumer effect preregistered as E26)

**Status:** CLOSED 2026-09-23 — built at `e0165f5` (one commit), verified O83 by an independent fresh-process probe: the served caveat equals the spec wording at 1,010 characters on the section and its `note` form, "YOU MUST READ" in no served string, the surface byte-identical to O79 and every other response unchanged. The consumer effect is E26's after-arm, run by this session on this build. History: ISSUED and held 2026-09-23 until E26's baseline existed (O82), RELEASED the same day, built within the hour. The order is ready to build the moment that section exists; built earlier, it destroys the baseline it will be measured against. One part; one commit.

**Part A — the caveat wording.** The `laws_indexed` `possibly_superseded.caveat` served by `get_us_code_section` becomes, character for character, the wording in `40-tools.md` "Staleness indicator" as of this order: "INDICATOR ONLY — NOT A FINDING THAT THE TEXT CHANGED. A listed law MENTIONS this section; that is all the index records. It may amend the section, amend something else and merely cite this one, waive it for a named party, or not yet be in effect. The verification set's own example: Public Law 119-74 is listed against 42 U.S.C. 2210 because one appropriations rider cites it in a parenthetical, and it amends nothing in the section. This server does not read enacting laws. The list may also be incomplete: the index misses about one in seven listed (law, section) pairs. What follows is for the tool caller, not the person asking. To find out whether the text changed, read the listed law before answering: call get_public_law with its package_id, then find this section in its text. If the listed law is not read, tell the person asking, in plain words, that a later law mentions this section and has not been checked — and do not pass on tool names, package ids or field names, which the person cannot use." The first six sentences are the served ones today; the incompleteness sentence moves up; the "YOU MUST READ" sentence is replaced by the last three. Nothing else changes: the `none_indexed` and `not_checked` wordings, the description's shorter warning, the server instructions and every other served string are byte-unchanged. The unit test that asserts the caveat equals the spec wording is updated to the new string, and a test asserts that "YOU MUST READ" appears in no served string.

**Verification artifacts, under the gitignored `runs/`:** the R8 trace of one `laws_indexed` call (`42 U.S.C. 2210`, `max_chars` 300) and one `none_indexed` call; the served `tools/list`; and a diff of every served string against the pre-build surface (expected: exactly one differing string). This session verifies with the O66 `fired.py` arm and a surface diff, then runs E26's after-arm.

## WO-15 — the code review's contract defects and the conversion cost (S26; contracts in `40-tools.md`, "Staleness indicator" and "By-id behavior"; measured basis O86)

**Status:** BUILT 2026-09-26 as seven commits `9b57f57`…`21bebbb` on top of `e266aea`; VERIFIED O87 (the review's instrument re-run, this session's fresh-process probes, the E27 after arm, a byte-identical `structure` block, identical ASTs across the reformat). CLOSED on parts A–E, G and H; part F's last artifact — a green workflow run with the format check present — is owed on the maintainer's push. Two implementation decisions ratified into `40-tools.md` (O87f): at least one segment after the package id; `prediction_note` on a cancelled speculative query. History: ISSUED 2026-09-25; amended the same day with parts F–H once Q23 was ruled (R26), and RELEASED. Eight parts, A–H: A–C are defects against sentences already in this spec and go first; D is efficiency with a preregistered before set (E27, O86a); E is documentation drift in files outside `documentation/`; F–H are the maintainer's rulings on the review's hygiene items. Verification artifacts are named per part; the order closes when they are in `90-observations.md`.

**What it is.** S26 — a read-only code review by the implementation session's own model at c9d2a0d, instrument offline, every reproduction captured (`runs/2026-09-26T024521Z-code-review/`) — reproduced three behaviors that contradict this spec and measured one cost. This order fixes those four and the documentation drift it found. The review's items that were the maintainer's to decide (CONTRIBUTING's wrapping rule, `ruff format` in CI, the `.env` walk-up, the provisional SDK seam) were Q23 and are ruled R26; they are parts F–H.

**Part A — a malformed detector body never fails the lookup.** A detector response whose `results` elements are not objects (the reproduction's body: `{"count": 1, "results": ["PLAW-119publ1"]}`) yields `possibly_superseded.status: not_checked`, `reason: malformed_response`, the body echoed, on a `success` lookup — the handling the search path already gives element-type drift. Guard every path the detector result takes after the response is read, not only the one the reproduction hit. Artifacts: a unit test with the reproduction's body and one with a `results` element that is an object missing every expected key; `instruments/repro.py` section 1 re-run reading a `success` lookup with `not_checked`.

**Part B — the granule id grammar.** `granule_id` is accepted only when it matches `USCODE-{4 digits}-title{digits}[a-z]?(-[A-Za-z0-9]+)*` (O86b: 540 of 540 GovInfo-served ids); anything else is `invalid_argument` naming the id and stating nothing was fetched, before any request. This is a character-class check and does not parse the id beyond the leading segments (WO-13's exclusion stands). Artifacts: a unit test that the reproduction's traversal id, a dotted id, a query-bearing id and a whitespace-bearing id are each refused with zero requests on the mock transport, and that ten real ids spanning appendix, `front`, and hyphenated-section forms are accepted; `repro.py` section 2 re-run showing no URL requested. This session will probe one refused id from a fresh process and confirm no trace record is written.

**Part C — `no_bound` is true.** Reset the bound-error state on every launch; in the `no_bound` branch of completion, cancel any live detector task; a remembered `currentthrough` that does not parse as a date is not remembered, so no speculative launch happens on it. After the fix the reproduction's second lookup either reports the detector result obtained on the payload's date or reports `not_checked` with a reason that is true, and the number of detector queries sent equals the number the response accounts for. Artifacts: a unit test for the two-lookup sequence asserting response and request count agree; `repro.py` section 3 re-run.

**Part D — the conversion cost.** Remove the per-character index map, or bypass it when there are no field markers, and compute normalized offsets at marker positions only. Nothing served changes: `structure` on sections identical before and after, offsets included — this is the check that matters — and `total_chars`, `returned_chars`, banner and `find` offsets unchanged. Not ordered: giving `get_public_law` a `structure` block, which is a contract question the spec has not taken up (it promises `structure` on sections only). Artifacts: `repro.py` section 4 re-run on the same synthetic law (time and peak memory); a unit test that the section fixture's `structure` offsets equal the pre-fix values; this session runs the E27 after arm and compares the 17 U.S.C. 107 `structure` block byte for byte with `runs/2026-09-25-e27-spec-probe/trace-before/`.

**Part E — README and CHANGELOG.** The README's banner sentence says `text.banner` carries the banner and `content` is payload-only, as served (O79, O81); the CHANGELOG's E12 entry is corrected or annotated as superseded; the directory spelling in the README config example is fixed; the test count is made current or removed. Artifact: the diff.

**Part F — CONTRIBUTING and the formatter (R26 a, b).** CONTRIBUTING's sentence "Commits, just like code, should be wrapped at 80 columns" loses "just like code", so commit bodies wrap at about 80 and code follows Ruff's 120 with no contradiction left. `ruff format` is adopted: one commit that only reformats (no other change in it), then `ruff format --check` added to the quality workflow beside `ruff check`. Artifacts: the CONTRIBUTING diff; the reformatting commit's hash; a green workflow run in which the format check is present and ran.

**Part G — `.env` only in a development checkout (R26 c).** The server reads a `.env` only when it runs from a repository checkout, and only the file at that checkout's root. It never walks up from the working directory, and an installed copy (a wheel, a site-packages install, any layout without the checkout's root) reads no `.env` at all — production configuration is the process environment alone. How the checkout is recognised is the implementation's call, stated in the commit message; the behavior above is the contract. The startup contract is unchanged: a missing or blank key exits before a server object exists, with the same message. Artifacts: unit tests that (1) a `.env` in a parent of the working directory is not loaded, (2) a non-checkout layout loads no `.env` even when one sits in the working directory, (3) the checkout root's `.env` still loads when run from elsewhere with that checkout, and (4) the no-key exit is unchanged in every layout; the README's configuration section saying the same in a sentence.

**Part H — the `mcp` pin and the dependency-bump rule (R26 d).** `mcp` is pinned exactly at 2.1.1 in `pyproject.toml`, with a comment beside the pin naming the reason: the tracer implements a middleware protocol the SDK labels provisional within 2.x, and the end-to-end trace test reaches a private attribute, so any bump is a deliberate act with those tests watched. CONTRIBUTING gains the rule that every dependency bump runs the full unit suite, trace tests included, before merge. Artifacts: the `pyproject.toml` and lockfile diff; the CONTRIBUTING diff.

**Not in this order.** Any change to served wording, the banner, pinned caveats or descriptions. Any change to `get_public_law`'s response shape. The `<br/>` double newline — fixing it moves every offset after the first such tag and so changes `structure`, which D must hold fixed; if wanted, it is its own order with its own before set. Any dependency bump beyond the pin itself. Writing under `documentation/`.
