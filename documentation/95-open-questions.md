# Open questions

Q1–Q5 are answered — rulings in `96-rulings.md`. E1, E2, E7, E8 and most of E4 were run 2026-08-29; outcomes in `90-observations.md` O10–O19. Numbering continues.

## For the maintainer

Q6. v2 candidates deferred by R1/R2, parked here so they aren't lost: (a) a `N Stat. M` citation resolver that returns a GovInfo STATUTE pointer without ingesting anything; (b) a higher-recall PLAW→USC join — the package-summary `references` array demonstrably beats the `uscodecitation` search field (25/33 sampled recall, O21), and OLRC classification tables sit above both; (c) OLRC release points for current-text fetch-by-citation without an index. None are v1. Additions from later measurements: (d) the bulkdata COMPS repository (Statute Compilations in USLM — certain laws maintained as amended, O26) as a possible laws-as-amended source. No maintainer action needed until a v2 conversation starts.

Q7 (E2E harness) is answered and ruled — R10 in `96-rulings.md`, maintainer answers verbatim in commit 198cd0c, contract in `60-e2e-harness.md`, manifest at `e2e-manifest.json`. Remaining threads:

Q7e-completion — CLOSED by R11 (2026-08-31): the harness became the standalone mcp-e2e project, the suite (manifest, groundings, scored findings) is spec-owned here, cadence is manual. Nothing left to confirm.

Q9 — COVERAGE COMPLETE 2026-09-16: the full run (`runs/2026-09-16T012953Z`, all 13 prompts × 3 cells, scored in `70-run-findings.md`) ran the A and D groups in the isolation cell for the first time, so the attribution clauses of A2/C2/D3 are scored (all pass). Score 38/39 (D2 isolation FAIL → F4). Layer 1: 15/16 (`recall-caveat-always` FAIL → F5). Findings F4–F6 routed to WO-5. Earlier runs for the record: floor over 11 prompts (021336Z, 10/11, F1), C1 re-run (023317Z), C3/E13 (024850Z), floor + ceiling + isolation over C1/C3/C4 (035906Z, 9/9). The watch:"none" workaround can revert to null once the harness null-fix ships. Remaining: Q8 Group F.

Q8. Group F needs 8–12 **verbatim** questions from real research sessions — congressMCP's sourcing rule, adopted whole: they must not be written by anyone who knows the internals, and the spec session is disqualified from authoring them by construction. The manifest ships with Group F empty and carries the caveat. Paste originals when available.

Q10 — **`possibly_superseded`: ship it, but the design question is not the one on the table.** You said you like the idea and would feed it here; here is what the spec already knows that changes its shape. The detector IS the join `50-public-law.md` explicitly declines to automate, for a measured reason (O42c): `uscodecitation` recall is 25/33 on the E9 sample (O21), structural, with a live instance in O29. That is survivable — but it means the flag can only ever be a **positive-only** signal. It may ADD a warning; it must never certify currency, because its silence is an instrument limit, not a fact (O42d). S11 named the couldn't-check and effective-date failure modes; it did not name this one, and it is the one that matters, because a caller's prior without any flag is correctly "this edition is 18+ months old" — a flag whose silence reads as a clean check actively degrades that prior. Concretely, three states, never two: `laws_found: [...]`; `checked, none found` carrying the O21 caveat in the same breath so absence is never reassurance; `could not check` (S11's `query_diagnostics` parallel, and correct). Second design point (O42e): the blind spot correlates with R4's motivating case — a note-codified Act will not match `uscodecitation` for the section it is printed under (O17/O18 is exactly 42 U.S.C. 2210), so `stripped_note: true` needs a deliberate branch and may need to say the check is weakest here. **What I need from you:** (i) confirm positive-only — the flag never implies currency; (ii) whether it fires on every `get_us_code_section` success or only on request, since it costs a search round trip per lookup; (iii) whether E16 should run BEFORE the build (it measures whether the flag is worth its round trip, and its false-negative rate on note citations) or whether you want it shipped on the strength of the positive signal alone and measured in the wild. I recommend (iii)-before: E16 reuses E9's methodology and answers both, and shipping first bakes the wording in.


Q10 — ANSWERED 2026-09-11, ruled R14 (`96-rulings.md`, maintainer text verbatim there): indicator only in both directions, fires on every lookup subject to the measured round-trip cost, E16 run now. Contract lands in `40-tools.md` once E16 is in.

Q11 — **OLRC: you asked whether to reopen R1.** R1 is in "Settled — do not reopen," so this needs your word, not mine. What has actually changed since you closed it, separated from what hasn't: **dissolved** — the "full sections only" objection, verified against the spec (O42b), since the granule is already the retrieval unit for an independent reason (O11); your later note that OLRC is a title-level bulk download plus an all-titles ZIP changes the shape of the work (fetch-and-cache a title, not per-section calls) but not that objection. **Not dissolved, and load-bearing** — the `structure` block is derived from GovInfo's own `field-start`/`field-end` markers with typed note vocabulary (O36), and OLRC USLM is a different vocabulary. Whether typed field extents re-derive from USLM at equivalent fidelity is the real gate: if no, the trade is a measured differentiating feature (R12b/R13, now validated by three consumers, O42a) for currency, which is a bad trade. That is a spike, not a project — but note that a bulk-download ingest is also the thing R1 ruled out in principle ("no OLRC release-point ingestion"), so even a successful spike needs you to reopen R1 rather than me to route around it. My recommendation matches S11's: don't reopen yet, run E16 first — if the staleness flag almost never fires on real lookups, the original decision is confirmed cheaply; if it fires often, you have a measured case AND you will know which titles to prioritize. **E16 is in (O43c):** twenty months after `currentthrough`, 3.8% of current-edition sections are listed by any 119th-Congress law, and the detector fired on 5 of 151 uniformly sampled sections. That is the "confirmed cheaply" branch — staleness is real, small, and now surfaced per lookup by R14. No case to reopen R1 on this evidence; the OLRC spike stays parked.

Q12 — **`fields=["statute"]` on `get_us_code_section` (S11's one uscode feature ask).** Semantic-unit retrieval instead of char arithmetic against extents the server already computes; S11 spent roughly a third of a window on header boilerplate and amendment notes it did not want. Cheap, since R13b already computes every field's `start_char`/`end_char`. Two things give me pause and neither is mine to settle. First, S11's own counterpoint, which I find the stronger argument: the footnote on 15 U.S.C. 261 ("So in original. Probably should be followed by 'by'") is what let it explain a strange insertion in the bill, and it says it would never have requested that field — so a fields filter optimizes away the material a consumer cannot know to ask for. Its proposed fix, footnotes attached by default even under a filter, is a special case that will not obviously generalize (which other fields are load-bearing-but-unrequestable?). Second, fields nest (O39/R13b) — `notes` encloses its typed children — so `fields=["notes"]` and `fields=["amendment-note"]` overlap, and the contract has to say what a request for both returns. Recommend deferring until a consumer is measured losing something to blind windowing that `find` + extents did not already solve; E14 closed because consumers stopped guessing, and this is a comfort improvement on a solved problem, not a fix. Your call.


Q13 — **R14b's round-trip gate, with the number (O43e).** You said fire on every lookup if the search round trip is under about 100 ms. Measured from this machine: the search's own cost is roughly 10–80 ms, but any request to `api.govinfo.gov` costs ~200 ms on a warm connection and ~500 ms on a fresh one, so a detector query is 210–280 ms wall-clock (median, keep-alive) and ~430–510 ms fresh. The threshold is met for the search and not for the round trip, so per R14b this is your call, not mine. The contract in `40-tools.md` is written to fire on every success with the query issued concurrently with the text fetch, which makes the common-case wall-clock addition roughly zero — that is what I recommend you confirm. If you would rather gate it, the alternative is an opt-in argument; the three-state object is the same either way and the implementation is queued on the fire-always shape.

Q13 — ANSWERED 2026-09-15 by the maintainer ("Q13 is answered"): the gate is met on the recommended reading — fire on every `get_us_code_section` success, detector query issued concurrently with the `txtLink` fetch. R14b closed on that basis; no opt-in flag. Contract in `40-tools.md` unchanged.

Let's fire it every time in parallel as written - the wall clock is nearly zero. All good.

Q12 — DEFERRED 2026-09-11, ruled R15. Maintainer agreed; the consumer session concurred with a stronger argument (S12 in `90-observations.md`, verbatim) that is adopted as the ruling's rationale, and its narrower reopening trigger is pinned there.

Q14 — **`get_us_code_section` has no id input, but its disambiguation message says "re-request by ids or year" (O45c).** Two granules can share a citation and an edition — the two `28 U.S.C. App. Rule 9`s (Appellate vs Civil rules, O24) — and nothing the tool accepts separates them, so the text of one of them is unreachable by this tool. Consumers have coped by asking the user which (A5, run 021336Z), which does not actually get them the text. Recommendation (IR judgment, but it adds a tool argument, so it is yours): accept an optional `granule_id` on `get_us_code_section` that bypasses the citation search and fetches that granule's `txtLink` directly — identity over string-matching is already the convention (`00-INDEX.md`), the ids are already in the disambiguation list, and no new upstream surface is needed. If declined, the message wording must drop "by ids". Either answer is a one-line ruling; the wording fix alone would be a trivial order.

Yep, let's allow the granule ID as an argument. It does change the schema, but optionally, so it's quite alright.

Q14 — ANSWERED 2026-09-15, ruled R16. Optional `granule_id` (with optional `package_id`) on `get_us_code_section`; contract in `40-tools.md`, build order WO-3.

Q15 — ANSWERED 2026-09-20, ruled R18 (`96-rulings.md`, maintainer text verbatim there): yes to all three; (c) goes in the GitHub Actions workflow. Contracts for (a) and (b) in `40-tools.md`; build order WO-10. The question as put follows for the record.

Q15 — **The audit's unruled findings (S15).** R17 disposed of findings 1 and 3. Three items came without a disposition, and none is this session's to decide, because each is a requirement about what the server protects against. They are claims about code this session does not read; what the spec can say about each: **(a) Finding 2 — the key follows upstream-provided URLs.** The audit says `X-Api-Key` is attached to every URL fetched, including download links taken verbatim from GovInfo responses. The spec is what makes this reachable: "Links are data, never constructed" obliges the server to follow whatever URL upstream hands it, and "Secret hygiene" says only that the key travels in the header — nothing says *to whom*. Every upstream download link in every artifact under `runs/` sits on `https://api.govinfo.gov` — 3,033 link values, one host (O56); none has been observed elsewhere, so the exposure needs a compromised or malformed upstream response. A contract is cheap and does not fight links-verbatim: the key is sent only to the `https://api.govinfo.gov` origin, a link on any other origin is not fetched with the key and is surfaced as its own failure, redirects are not followed with it. Recommend yes; it would be a one-paragraph addition to "Secret hygiene" and a WO. The allowed origin is a measured set of one (O56). **(b) Finding 4 — trace files under the ambient umask.** R8 made the trace a harness instrument, enabled only by an environment variable; the audit's concern is other local users reading queries and retrieved text on a shared machine. Retrieved text is public law; the queries are the sensitive part. Mode `0600` on creation is cheap; the audit's symlink and collision handling is more machinery, and the check-dead-defensive rule asks whether a shared multi-user host is a deployment you have. Recommend `0600` only, unless you run this on shared machines. **(c) Dependency auditing in CI** (pip-audit against PyPI and OSV on commits and on a schedule). Repo tooling outside `documentation/`, so it would go to the implementation session as an order with no contract behind it. Your call whether it is wanted at all. **What I need:** yes/no on each of (a), (b), (c).

Q16 — ANSWERED 2026-09-20, ruled R19 (`96-rulings.md`, maintainer text verbatim there): yes. Built as WO-11 part G. The same answer moved S17 finding 6 (the unclosed client) from noted to ordered, WO-11 part F. The question as put follows for the record.

Q16 — **Should CI run the unit suite and Ruff? (S17; left open by WO-10 part C.)** The only workflow is the dependency audit. WO-10 said whether it should also run the tests and `ruff` "was not asked and is not ordered"; S17 now reports the gap from the other side — 451 tests and a clean Ruff run locally, neither enforced on push or pull request. It is a requirement about the repository's process, so it is yours. Recommendation: yes, as a separate workflow (so an advisory-database outage cannot mask a test failure, or the reverse), offline only — the suite mocks upstream, and nothing in CI should hold the GovInfo key. If yes, it becomes a part F on WO-11 or its own order.

Q17 — ANSWERED 2026-09-20, ruled R20 (`96-rulings.md`, the consumer's replies verbatim there, adopted by the maintainer): (a) no smaller default, revisit if measured; (b) no trimming or relocating of guidance without a run on both sides; (c) the `laws_indexed` caveat stays. The question as put follows for the record.

Q18 — ANSWERED 2026-09-21, ruled R21 (`96-rulings.md`, maintainer text verbatim there): run it, under the loop driver, as part of a complete floor run. Part (ii), the environment variable, was not answered separately and R21 records the reading that the yes covers it. Preregistered as E22; switch ordered as WO-12 part F; cells in manifest version 4. The question as put follows for the record.

Q18 — **Which banner carrier goes, if either (S19, O65).** You and the consumer asked why WO-12 dropped `text.banner` and not the line in `text.content`. The answer is that I had no good reason: I dropped the one the spec had never mentioned. The traces say more than that — the two carriers have never been served apart, 469 of 469 truncated responses since the E12 build carry both and the 18 before it carry neither, so E12's confirmation is of the pair, and F1 was a 100K window the driver probably never delivered (O47). Part C is withdrawn, both stay, and `text.banner` is now specified as served. Your direction — keep the field, drop the content line — is the better design if it holds: `text.content` becomes pure payload, R13c's coordinate disclaimer goes, and quoted text cannot carry the banner with it. What it needs is the arm that has never been run: C1 at the floor and under the loop driver on `gpt-oss-120b`, at the 20,000 default, field-only against both. That needs a way to serve field-only before it is the contract, which means a harness-only switch on the server — a small contract addition I would rather you chose than I slipped in. **What I need:** (i) whether you want that experiment at all, given it costs a switch and runs, or would rather leave both carriers at under 80 characters and close this; (ii) if yes, whether a harness-only environment variable in the manner of `USCODE_MCP_TRACE_DIR` is acceptable as the switch. I would preregister it as E22 before any build. I lean (i)-yes only if a loop-driver run is happening anyway; on its own this is 80 characters.

Q17 — **S18: what to do about the default window, and whether to trim the descriptions (measured, O64).** A consumer asked about fair use, made one call on `17 U.S.C. 107` with `max_chars` 6,000, and reported back. Everything it said about the served surface checked out (E21, O64), and the parts that are technical judgment are already ordered as WO-12: spec codes out of served strings, 760 characters of source indentation out of the descriptions, the unspecified `text.banner` field dropped, and the description taught that a locating call can be small. Three things are yours. **(a) The default first window.** S18 asks for `fields=["statute","sourcecredit"]`, or for the first window to default to statute plus structure when notes dominate. `fields` is R15 and S18 does not meet its reopening trigger — its statute was 1,033 characters and arrived whole — so that half is declined on the existing ruling. The second half is a different thing and is R15's own pre-committed direction: no request vocabulary, just a smaller default. Measured on the uniform 151: 13 sections (8.6%) exceed the 20,000 default, and in 11 of those 13 `statute` and `sourcecredit` end inside it; the median section is 2,475 characters and the default never bites. So a rule like "when `max_chars` is omitted and the payload exceeds the default, the window ends where `sourcecredit` ends" would touch about one section in twelve, save up to ~18K on those, and needs a fallback for the 17 of 151 sections that have structure and no `statute` field and an exception for `note` citations, where the notes are the point (R4). Recommend **no, not yet**: S18 lost nothing — it capped the window itself, and WO-12 part E teaches every consumer to; a smaller default makes more responses truncated, and truncation is where the one measured consumer failure of this kind happened (F1); and notes are S12's "material whose relevance can't be known before reading". The trigger I would pin instead: a harness run or field report in which a consumer takes the default on a note-heavy section and is measured paying for it. Uniform sections are not asked-about sections, so the 8.6% is a floor on traffic, not an estimate. **(b) Trimming the descriptions.** The served surface is 12,518 characters, about 3K tokens per session; WO-12 takes out roughly 800 without touching meaning. Going further means cutting guidance, and every block in `get_us_code_section`'s 4,418 characters is there for a measured consumer failure (F1, F4, F6, F7, O38, O44g). S18 itself says the guidance "did shape my behavior correctly". Recommend **not without a run on both sides**: if you want it, it is a preregistered experiment — a trimmed description against the current one over the full manifest, floor cell included — and the cadence of runs is yours (R11). **(c) The `laws_indexed` caveat.** S18 says trim the wording, not the check. It only saw `none_indexed` (419 characters, and the whole object is 905 — smaller than `structure`). The long one is `laws_indexed`, which is your wording, pinned verbatim (R14a addendum), and S18 credits the caveat's strength for the fact that it hedged. Recommend leaving it; say so if you want a shorter one drafted. **What I need:** yes/no on (a) and (b); (c) only if you disagree.

## Implementation queue

**Order of events from here (2026-09-21):** WO-12 is handed to the implementation session (parts A, B, D, E, F); this session verifies it by re-running E21's arms 1–3 and a three-mode probe of the switch; then the maintainer runs manifest version 4's eight loop cells, which is at once the first complete loop-driver run, E22 and E17; this session scores from the artifacts. Nothing is owed by the maintainer before the build.

**WO-12 DRAFTED 2026-09-20, not yet issued to the implementation session** — served-string fixes from S18 as measured by E21 (O64): spec codes out, source indentation out, one window convention, the small locating call taught. Part C (`text.banner` dropped) was withdrawn before issue (S19, O65). Q17 is ruled (R20). Q18 (which banner carrier, if either) is open and WO-12 does not wait on it. The paragraphs below are earlier states.

**Queue empty, nothing owed.** WO-11 CLOSED in full 2026-09-21: part G's first run is in — `success`, 500 tests on CPython 3.11.16, Ruff clean (O63). Parts A–D measured and E–F closed as unit-level claims (O62). S17 is fully disposed of. The paragraphs below are earlier states.

**Queue empty, nothing owed.** WO-10 part C CLOSED 2026-09-20 (418d644, O60): the repository gained its GitHub remote and the dependency audit's first run passed 4 of 4 jobs, 43 locked packages against PyPI and OSV; the weekly and pull-request triggers are unobserved until they fire. The audit (S15) is fully disposed of: finding 1 accepted with a README warning (R17b), finding 3 fixed (WO-8, WO-9), findings 2 and 4 and the CI recommendation built (R18, WO-10). (The second item owed at O59, the name of the API-base setting, was answered by the maintainer the same day, S16: there is none.) WO-9 CLOSED 2026-09-20 (becc5f2, O59): the quote-borne bypass is shut, 15 of 15 predicted rows now refused, 37 unchanged. WO-10 parts A and B CLOSED 2026-09-20 (440fb8e, 8d55b75, O59): key bound to the API origin — regression measured, refusals unit-test-only by nature; trace files `0600`, measured under `umask 000`. WO-8 CLOSED 2026-09-20 (a4e4e04, O57) as built to its order: refusal shape, no upstream request on refusal, README warning. WO-7 CLOSED 2026-09-19 (c373a2e, O54) — `public_pdf_link` and `details_link` in provenance, the audience sentence carrying the public link, every served link opened without a key. Before it: WO-6 CLOSED 2026-09-19 (ee4ca88, O52); WO-5 CLOSED 2026-09-17 (4f238a6, O50); WO-4 CLOSED 2026-09-16 (3967e66, O48); WO-3 CLOSED (d15ddde + bb73359, O46); WO-2 CLOSED (faa9974, O45); WO-1 CLOSED (1d42c52, O44). The full run is scored (`70-run-findings.md`, 38/39). Owed by a future run: `recall-caveat-always` should now pass, and D2/isolation measures whether F4's description fix holds. Owed by the next loop-driver run, now unblocked on the server side: E17 (two pinned arms, distinct-answer counts, manifest version 3's C1, the public link counted). Q8 Group F questions remain with the maintainer.

Harness-side (mcp-e2e, not the server): the finish reason on every wire line (the harness's own WO-3, S13) is what E17 and the cap-cut scoring rule wait on, and the harness's `uscode-loop-manifest.json` still carries version 1's C1 (O51). E15's api-surface extension — DELIVERED and verified from artifacts (O47d); the O40b duplication reading and F3's disposition are both settled on it (O47a/b).

Previously: R12a/b, the E12 banner, and the keyless fail-fast are built, black-box verified (O37), and consumer-validated: E12 and E14 both CLOSED as confirmed by run 2026-09-02T035906Z (`70-run-findings.md`) — banner in every truncated trace with honest totals in all three cells, find used everywhere with zero blind guessing, verbatim-verified quotes, and the C3 non-note control at 4 floor calls against the 9-call baseline. The deferred notes-only mode is dead (E14 was its reopening condition). Remaining open board: A/D prompt groups have never run in the isolation cell (attribution clauses of A2/C2/D3 unscored), and Q8's verbatim Group F questions.

## Preregistered experiments — pending

E22 (preregistered 2026-09-21, before the switch is built and before any run; from S19, O65, R21). **Question:** which banner carrier, if either, can a weak consumer do without? The line that opens `text.content` and the `text.banner` field have never been served apart (O65), so nothing measured says which one works. **Instrument:** manifest version 4 under the harness's loop driver — `openai/gpt-oss-120b`, `deepinfra/bf16`, `reasoning.effort: low`, `temperature: 1.0`, `max_tokens: 8192`, `loop-scaffold@1` — three arms in each of two roles: `both` (`loop-floor`, `loop-isolation`), `field` and `content` (the four `…-banner-…` cells), the arms differing by the server's `USCODE_MCP_BANNER_CARRIERS` and by nothing else, on the server as built by WO-12. The primary prompt is C1 (`I need the full text of Public Law 118-31`, a 3.59M-character law at the 20,000 default — the F1 and F7 prompt); C2–C4 run in every arm and are scored under their pinned criteria as a secondary read. **Ten invocations of C1 per cell.** The unit of count is distinct answers among rows whose wire finish reason is the model's own stop (`60-e2e-harness.md`, cap-cut rule; the finish reason is on the wire since the harness's WO-3); invocation counts are stated beside them. **Arm validity, checked from the traces before anything is scored:** every truncated response in a `field` cell has `text.banner` and a `text.content` that does not open with `[WINDOW`; in a `content` cell the reverse; in a `both` cell both — the O65 census, per cell. An arm whose traces do not show its carriers is not an arm and is not scored. Under `field` the `banner-leads-truncated-content` check fails on every truncated record by construction; that failure is the arm's signature and is not a finding. **Measures, per arm and role, pinned now.** M1, the F1 clause: distinct answers that present a window as the whole law, or state a total size that is not the true total. M2: distinct answers that PASS C1 under the version-3 criteria, which version 4 carries unchanged. M3: distinct answers whose `answer.txt` contains the literal `[WINDOW chars`. M4: distinct answers whose `answer.txt` contains `start_char`. M5: distinct answers that give the `public_pdf_link` URL. M1 and M2 are scored by reading; M3–M5 are string tests and are computed by script. **Expected.** M1 is zero in every arm: `text.message` states the payload size in all three, and the 100,000-character window F1 failed on is gone (O47, O65c). M3 is nonzero under `both` and `content` and zero under `field`, where the string is not in anything the model would quote. M4 does not differ by arm, because the continuation the consumer relays comes from `text.message`, which every arm carries — so the parroting the maintainer saw is the message's, not the banner's. M2 and M5 do not differ by arm. If all of that holds, the content line buys nothing and costs a coordinate hazard. **Decision rule, pinned now; a difference of one distinct answer is a tie.** (i) `field` no worse than `both` on M1 and on M2 in both roles: the content line goes — `text.content` becomes pure payload, R13c's sentence is retired, the banner check is repointed at `text.banner` in a new manifest version, and the switch is removed. (ii) `field` worse than `both` on M1 or M2 by two or more in either role, and `content` not: the field goes, the content line stays — WO-12's withdrawn part C, now with a reason. (iii) both single-carrier arms worse than `both`: both stay, and the redundancy S18 reported is what the floor needs. (iv) `content` and `field` both no worse than `both`: rule (i), on design grounds, since nothing separates them and a pure payload is the better contract. **Falsifiers of the expectation:** any M1 failure under `field` that `both` does not show — then F1's theory that a weak model reads the text and not its siblings holds at the 20,000 default and the content line is doing the work; or M4 lower under `field` by two or more — then the banner, not the message, is a source of the relay, which is the maintainer's reading and against this session's. **What ten draws cannot show:** a failure rate under about one in ten; the rule acts on gross differences only, and a tie under (i) or (iv) is a decision made on design grounds with no measured harm, not a measurement of no harm. One model, one provider: a second lineage could differ, and the rule's outcome is revisited if a second-lineage cell later disagrees. Outcome to `90-observations.md` and `70-run-findings.md` either way.

E17 amendment (2026-09-21, before any run; R21). E17 runs on this suite's manifest, version 4, cells `e17-deepinfra` and `e17-akashml` — C1 only, knobs identical to the O51 baseline (no sampling knob, `max_tokens` 4096), AkashML tag `akashml/bf16` read from the endpoint listing 2026-09-21. Version 4's C1 is version 3's byte for byte. The harness's copy of the manifest is superseded, so the re-sync E17 was waiting on is moot; the finish reason is on the wire (harness WO-3, accepted 2026-09-18). The server under test is the WO-12 build with the switch unset. One confound to state rather than hide: WO-12 part D changes one clause of `text.message` ("chars 0-20000" becomes the banner's inclusive form) between the O51 baseline and this run, alongside WO-6 and WO-7's sentence. Expected and falsifier otherwise unchanged.

E21 — RUN 2026-09-20, CLOSED, outcome O64 (artifacts `runs/2026-09-20-e21-spec-probe/`). All four expectations confirmed, no falsifier fired: surface 12,518 characters; `O43b`/`O21`/`O36` and `O21, O17/O18` served, two of three paths traceable to this spec's own WO-1 wording; `17 U.S.C. 107` is 30,175 with `statute` 1,033; a `max_chars` 1 look costs 3,791, so S18's "no cheap look" is a description failure, not a missing feature; an unspecified `text.banner` is the third copy; 13 of 151 sampled sections exceed the default and `statute` alone exceeds it in 2. Found outside the preregistration: 760 characters of served source indentation, and an inclusive/exclusive mismatch between banner and message. Disposition: contract additions in `40-tools.md`, WO-12, Q17. Preregistration follows for the record.

E21 (preregistered 2026-09-20, before any probe; from S18 — a maintainer-relayed consumer field report, its statements about served strings and sizes all claims). **Question, four arms — what S18 says about the served surface, and the one thing it could not see: how typical its section is.** (1) Description cost: the character length of the served `instructions` string, each tool's `description`, and each tool's input schema serialized as compact JSON, with the total. (2) Spec-internal identifiers in consumer-facing strings: every string in the served surface (arm 1) and in every arm-3 response, scanned for `\b[OREFSQ]\d{1,3}[a-z]?\b` and `WO-\d+`, each match reported with the path of the string it sits in. (3) S18's call and its neighbours, on `17 U.S.C. 107`: `max_chars` 6,000 (S18's call); no `max_chars` (the default it avoided); `max_chars` 1 (the cheapest look at `structure`); and a reading call from `statute`'s `start_char` for exactly its extent. For each: the size of the structured envelope as compact JSON, broken down by top-level key; `total_chars`; the `statute` and `sourcecredit` extents; the `possibly_superseded` status and the object's size; every path at which the window bounds or the continuation offset is stated, and whether a `banner` field exists apart from `text.content`'s first line. (4) Corpus shape: for each of the 151 uniformly sampled current-edition sections of O43c (every 401st of 60,173 section granules in walk order; ids in `runs/E16-2026-09-11/fire.json`), one `get_us_code_section` call by `granule_id` with `max_chars` 1, recording `total_chars`, whether `structure` is present or omitted, and the end of `statute` and of `sourcecredit`. **Instrument:** the fresh-process stdio probe with the R8 trace on (the O52/O54 recipe), implementation at `b9d9185`, outputs in a new gitignored `runs/2026-09-20-e21-spec-probe/`. Arm 4's denominator is the 151 ids in that file; a call that does not return `success` is counted and listed, never dropped. **Expected.** (1) `get_us_code_section`'s description is the longest of the four and exceeds 3,000 characters; the whole surface exceeds 12,000. No measurement of it exists — the descriptions grew by WO-1, WO-3, WO-5 and WO-11, each addition for a measured consumer failure, and nobody has added them up. (2) Matches present, and at least `O43b` and `O21` trace to this spec, not to the build: WO-1's table describes the `none_indexed` caveat as "…one in seven listed (law, section) pairs (O43b) and one in four in an earlier sample (O21)", citations written for the implementer inside a sentence that reads as wording. The pinned `laws_indexed` caveat and the reference instructions text carry none. `O36` is expected in `structure.note` or a description, origin unknown. (3) `total_chars` near 30,000 with `statute` ending near 1,000, as S18 says; `possibly_superseded` between 1,000 and 2,500 characters and byte-identical on the reading call; the `max_chars` 1 call succeeds and carries `structure`, so a look before reading exists at the envelope's fixed cost — which would make S18's "no cheap way to look before reading" a teaching failure of the description, not a missing feature. On the three-copies claim the spec requires two carriers, the banner line in `text.content` (F1) and `text.message` (F7, R13c); a separate `banner` field is nowhere in `40-tools.md`, so if one is served it is an unspecified third. (4) Most sections are small: median `total_chars` under 5,000, fewer than 15% over the 20,000 default, and `statute` alone over 20,000 — R15's reopening trigger — in fewer than 3%. **Falsifiers.** (1) A whole surface under 6,000 characters: the cost S18 reports is not there and the item is dropped; between 6,000 and 12,000 is judgment and will be recorded as such. (2) Zero matches: S18 misattributed the codes and the item is dropped. (3) `total_chars` or the `statute` extent off S18's figures by more than a quarter; the `max_chars` 1 call refused or served without `structure` — then S18 is right that no cheap look exists and that is a contract gap; window bounds stated at two or fewer paths. (4) More than half the sample over 20,000 characters — the default window would be mis-sized for the corpus, not only for well-known sections; or `statute` alone over 20,000 in more than 3% — R15's trigger would be live in the corpus. **What arm 4 cannot show:** a uniform sample of sections is not a sample of the sections people ask about, which skew toward the famous and the note-heavy — `17 U.S.C. 107` and `42 U.S.C. 2210` are both in this spec for that reason. It bounds how much of the corpus a default-window change would touch; it says nothing about how much of the traffic. **Not measured here:** whether any consumer reads the default window's notes to its cost. S18 did not — it capped the window itself. Outcome to `90-observations.md` either way.

E20 — RUN 2026-09-20, CLOSED, outcome O61 (artifacts `runs/2026-09-20-e20-spec-probe/`). Arm 1 confirmed, 60 of 60 collide. Arm 2: the preregistration left the served behavior open and S17's version of it is FALSIFIED — no private law is served, 0 of 60; all 60 public laws answer `ambiguous` instead, which is the worse defect. Arms 3, 4, 5 confirmed as S17 claimed. Disposition: contract corrections in `30-search.md` and `40-tools.md`, WO-11, Q16. Preregistration follows for the record.

E20 (preregistered 2026-09-20, before any probe; from S17 — a maintainer-relayed code review by another model, all of it claims, its instrument offline and self-fed). **Question, five arms — the parts of S17 that can be seen from outside.** (1) Upstream: does `collection:PLAW congress:{n} docnumber:{m}` return a private law alongside the public one when both series have that number? `30-search.md` says `lawtype:public` joins the resolution query "only if a collision is ever observed; none has been" — nobody looked; O12 measured one pair, 118/31, where no private law 31 exists. (2) Served: what does `get_public_law` with `congress` + `law_number` return on a colliding pair? (3) Upstream and served: is the `uscodecitation` field name case-insensitive, and which `recall_caveat` does `search_public_laws` attach to `USCODECITATION:"42 U.S.C. 2210"`? (4) Served: `get_us_code_section` with `granule_id: USCODE-2024-title17-chap1-sec107` and `citation: "42 U.S.C. 2210"`. (5) Served: `max_chars: 0`, `start_char: -1`, and a `start_char` past `total_chars` on both text tools — outcome, and wall-clock against a valid call on the same input as the only outside view of whether upstream work preceded the refusal. **Instrument:** direct `POST /search` with the key in the header for (1) and (3); the fresh-process stdio probe with the R8 trace on (O52/O54 recipe) for the served arms; outputs in a new gitignored `runs/2026-09-20-e20-spec-probe/`. Arm 1's denominator is every package under `collection:PLAW lawtype:private`, enumerated at run time (60 at O44e); for each, the bare query and the same query with `lawtype:public`, recording `count` and the packageId set — identity, not titles. Arm 2 runs on every colliding pair arm 1 finds, `max_chars` small. **Expected:** (1) every private law collides — `count: 2`, `{publ{m}, pvtl{m}}` — since private numbering restarts at 1 each congress and every congress passes more public laws than private; with `lawtype:public`, `count: 1`, the `publ` package, every time (O44e's partition). (2) Unknown which hit the server takes; S17 says the first. Any `pvtl` package returned as `success` is an R6 violation served, and it is this spec's defect before it is the build's — the contract pins the query without `lawtype`. (3) Case-insensitive upstream, as the `collection` field name was (O55b), count equal to the lowercase form's; served caveat is the full-text wording, per S17. (4) `success`, title 17's text, `normalization` and `possibly_superseded.checked_citation` saying `42 U.S.C. 2210`, per S17. (5) `invalid_argument` for the first two with wall-clock near a valid call's rather than near zero; past-the-end is `success` with empty content and `truncated: false`. **Falsifiers:** (1) the bare query returns only the public law for every pair — then finding 1 is unreachable and `30-search.md` stands; (3) uppercase field name returns zero or an error — then the caveat selector is right to ignore it and finding 5 is dead-defensive; (2), (4), (5) — any served behavior other than the claimed one means S17 misread the code and the finding is dropped or restated from the trace. Findings 3 and 6 are not observable from outside and stay claims whatever this shows. Outcome to `90-observations.md` either way.

E19 — RUN 2026-09-20, CLOSED, outcome O58: 15 of 15 link kinds HTTP 200 with no `Location`, `POST /search` 200; expectation confirmed, the never-follow-redirects rule is free. Preregistration follows for the record.

E19 (preregistered 2026-09-20, before the measurement; for R18a, the key-origin contract). **Question:** does any upstream download link answer with a redirect? A contract that says the key is never carried across a redirect is free only if no fetch the server makes needs one. **Instrument:** `curl` with the key in the header, redirects not followed, status and `Location` recorded, against links taken verbatim from today's traces: a USCODE granule `txtLink` (`/htm`), its `pdfLink`, `modsLink`, a granule `summary` link, a PLAW package `txtLink`, `pdfLink`, a `uslmLink`/`xmlLink` where the trace carries one, and `POST /search` itself. **Expected:** every one 200 with no `Location` — the served server works today and S15 claims its redirects are disabled, which is a claim about code. **Falsifier:** any 3xx — then the contract has to say what happens when the `Location` is on-origin versus off-origin, and the target hosts get recorded. Outcome to `90-observations.md` either way.

E18 — RUN 2026-09-20, CLOSED, outcome O55 (`90-observations.md`; artifacts `runs/2026-09-20-e18-spec-probe/`). Arm 1 confirmed: the bypass is real in both directions and reaches BILLS. Arm 2: field name and value case-insensitive, quoted value honored, the parenthesized OR returns both collections as expected; not expected — repeated collection terms union (so prepending cannot scope, and the contract refuses instead), and the space-after-colon form is not a clause at all. Arm 3 FALSIFIED: a top-level OR does not escape a prepended clause, so the contract does not require grouping; the first arm-3 instrument 500'd and is recorded as an instrument failure, not a result. Disposition: R17, "Collection scoping" in `40-tools.md`, WO-8. Preregistration and amendment follow for the record.

E18 (preregistered 2026-09-20, before any probe; from S15 — a maintainer-relayed security audit by another model, finding 3, a claim). **Question, two arms.** (1) Served behavior: does each search tool run a query that names the *other* collection? `40-tools.md` as written says the server prepends its collection "unless the query already contains a `collection:` term", so the spec itself licenses it; the audit says the implementation does exactly that. (2) Upstream syntax: which spellings of a collection clause does GovInfo honor — these decide what a validator has to recognize, and a validator authored from a guess about the syntax is dead-defensive on one side and porous on the other. **Instrument:** arm 1 is the fresh-process stdio probe (O52/O54 recipe) with the R8 trace on: `search_us_code` with `collection:PLAW congress:118 docnumber:31`, `search_public_laws` with `collection:USCODE citation:"17 U.S.C. 107"`, `search_us_code` with `collection:BILLS congress:118`, and one same-collection control per tool; the collection of each hit is read from its `packageId` prefix (identity, not title). Arm 2 is direct `POST /search` with the key in the header: `collection:uscode` (lowercase value), `COLLECTION:USCODE` (uppercase field), `collection:(USCODE OR PLAW)`, `collection:USCODE collection:PLAW`, `collection: USCODE` (space after the colon), and `collection:"USCODE"`, each conjoined with `"fair use"`, against the control `collection:USCODE "fair use"`; record `count` and the packageId prefixes of the first page. **Expected:** arm 1 — the bypass is real in both directions and `BILLS` hits come back from `search_us_code`. Arm 2 — values are case-insensitive (the congressMCP traces under `runs/govinfo-search/` show `collection:bills` sent; whether it matched there is not something this session has measured), the parenthesized OR returns both collections; the other three unknown, no expectation. **Falsifier:** arm 1 — every hit of a foreign-collection query carries the tool's own prefix, or the call is refused: then the audit's finding 3 is wrong about the served server and only the spec text needs correcting. Arm 2 — any spelling expected to work returning zero or an error narrows the validator; any spelling beyond these six found to work widens it. Outcome recorded in `90-observations.md` either way.

E18 amendment (2026-09-20, after the first probe, before the second; the first probe's results are already on disk in `runs/2026-09-20-e18-spec-probe/00-summary.json` and are not reinterpreted by this). The first probe measured repeated `collection:` terms as a union, not an intersection. If terms union, a query with no collection clause at all may still escape a prepended scope through a top-level `OR`. **Arm 3, upstream direct:** `collection:USCODE "fair use" OR docnumber:31`, `collection:USCODE ("fair use" OR docnumber:31)`, `collection:USCODE AND ("fair use" OR docnumber:31)`, `collection:USCODE ("fair use")` against the 11-hit control, `collection:USCODE (collection:PLAW "fair use")`, and `collection:USCODE -collection:PLAW "fair use"`; **served:** `search_us_code` with `"fair use" OR docnumber:31`. Prefixes read over a 100-hit page, since a foreign hit can sit below the first ten. **Expected:** the unparenthesized OR leaks non-USCODE hits, served and direct; parenthesizing the caller's query closes it and leaves the control at 11. No expectation on the nested foreign clause. **Falsifier:** no leak from the bare OR — then grouping is unnecessary and the contract should not require it; or parentheses change the control's count — then grouping is not a free fix.

E17 (preregistered 2026-09-18, from F7/O51; the harness spec session's two amendments, relayed via S13, folded in before any run). **Change under test:** on every truncated text response (`get_public_law` and `get_us_code_section`), `text.message` gains a sentence addressed to the audience problem — the `start_char` continuation is for the tool caller, and a person who wants the whole document should be given the PDF link, with the URL from `provenance` written into the sentence (`40-tools.md`, "No silent truncation"; build order WO-6). The banner is unchanged: its exact text is pinned by the `banner-leads-truncated-content` check and O37. **Why this shape:** F1 closed when the truth moved into the stream the model reads (E12); F7's audience relay is the same failure class one field over — 21 of 21 loop-driver C1 answers repeated `text.message`'s continuation to the asker (O51c), so the message should say the right thing to repeat. **Instrument:** the harness's loop driver, C1 under manifest version 2, model `openai/gpt-oss-120b` at low effort, same knobs as O51 (no sampling knobs, `max_tokens` 4096), two pinned arms: DeepInfra/bf16 (the O51 arm, baseline in hand) and a second pinned provider — AkashML is the candidate, the provider that offered the link in 4 of 7 unpinned rows (O51c); what quantization the harness's pin can hold there is the harness's to state — so a change on the DeepInfra arm can be separated from a provider's prior. Ten invocations per arm; cap-cut rows set aside per `60-e2e-harness.md`. **Unit of count is distinct answers, not invocations** (amendment 1; O51d: ten pinned invocations gave six distinct answers, so "0 of 10 to a majority" on that arm is in practice "0 of 6 to 4+ of 6"). **Expected:** on the DeepInfra arm, distinct answers that give the asker the PDF URL inline go from 0 of 6 (O51c/d) to a majority of distinct answers, and distinct answers that relay `start_char` to the asker drop below half. On the AkashML arm, movement in the same direction from its unpinned prior (4 of 7 invocations mentioned the PDF, 3 of 7 with the URL). **Falsifier:** no change in distinct answers on the DeepInfra arm — then the relay is model-dominated, the message change stays (it is correct and costs nothing), and the version-2 audience clause remains the only instrument, scoring the model rather than the server. **Confound falsifier (amendment 2):** the AkashML arm moves and the DeepInfra arm does not — a provider effect, recorded as such, not claimed as a server effect. **Not expected to change:** the self-truncation at offset 6,717 and the 20,000-character claim (O51a) — the server cannot stop a model cutting its own paste; if that moves, it is logged as a surprise, never claimed as an effect. **Before running:** the harness's loop manifest must carry version 2's C1 (`60-e2e-harness.md`, Manifest versions), and the finish reason should be on the wire (S13) so cap-cut rows are separated by measurement rather than by the token-count inference.

E17 amendment (2026-09-19, before any run; from O53). The link counted is `public_pdf_link` — an answer carrying only the API-host `pdf_link` counts as no link, because the asker cannot open it (401 without the key, O53a). E17 runs only after WO-7 has landed and been verified, under manifest version 3 — WO-7 landed (c373a2e) and was verified (O54) on 2026-09-19, so E17 now waits only on the harness side: the loop manifest re-synced to version 3 and the finish reason on the wire (S13). Expected and falsifier otherwise unchanged.

E16 — RUN 2026-09-11, outcome O43 (`90-observations.md`). Against preregistration: fire rate above zero but low (5/151 uniform, 3.8% of the corpus listed); false-negative rate 14.3% (20/140), BETTER than O21's 24%, so the trustworthiness falsifier did not fire and R14a's indicator wording stands on its own merits rather than by necessity; note stratum unmeasurable (references carry no note label) — one note-heavy section, 42/2210, gives 1 of 2 laws; latency above the R14b threshold on round trip, below it on search cost → Q13. `approveddate` ranges 500 upstream; the contract uses `publishdate`. Original preregistration and amendments follow for the record. E16 (preregistered 2026-09-07, from S11/O42; gates Q10 and informs Q11). **Question, in two parts from one protocol:** (1) how often would a `possibly_superseded` detector fire on realistic section lookups, and (2) what is its false-negative rate — the share of sections genuinely touched since `currentthrough` that `uscodecitation` fails to surface. Part 2 is the one that decides the flag's wording, and it is the part neither S11 nor the maintainer proposed measuring. **Instrument:** reuse E9's methodology (O21), which already established package-summary `references` arrays as the higher-recall ground truth against the same field. Take the enacted public laws since 2025-01-06 (the 2024 edition's `currentthrough`, O5/O15); build the true (law, section) set from their `references` arrays; then for a sample of sections drawn from that set, run the detector query as specified and record hit/miss per section. State the denominator and where it came from, per convention. **Expected:** fire rate materially above zero (the maintainer's own framing — the codified layer runs 18+ months behind by design, R1), and false-negative rate in the neighborhood of O21's 8/33 (~24%). **Falsifier of the flag's value:** a fire rate near zero on real-shaped lookups — then staleness is rare enough in practice that the flag adds a round trip and a caveat for nothing, and R1's original acceptance is confirmed cheaply (this is S11's "spent an afternoon confirming it" outcome). **Falsifier of the flag's trustworthiness:** a false-negative rate materially WORSE than O21's, especially on note citations — then positive-only wording is not merely advisable but the only defensible shape, and Q10(i) answers itself. **Stratify by citation type:** note citations vs plain section citations, separately reported, because O42e predicts the detector is weakest exactly where R4's motivating case lives and that prediction should be measured rather than assumed. **Note the dependency:** this needs a live GovInfo key and network; it is a spec-session measurement of the public API, not implementation work.

E16 amendments (2026-09-11, pinned before the run, after R14 and one instrument-shape probe): **(A) Ground-truth frame.** "Laws enacted since 2025-01-06" is operationalized as every `collection:PLAW congress:119` package with a `publ` id (the 118th Congress ended 2025-01-03, so every 119th law postdates the 2024 edition's `currentthrough`; private laws excluded per R6); denominator is that search's `count` and the enumerated list, and every package's `dateIssued` is checked against the cutoff so the proxy is verified rather than assumed. Ground truth per law is its package summary's `references` array flattened to (title, section) pairs exactly as E9 did. **(B) Recall arm.** E9's sampling rule, unchanged: first, middle, and last pair per law, plus (42, 2210) wherever present, membership-tested by `collection:PLAW congress:119 docnumber:{n} uscodecitation:"{t} U.S.C. {s}"`; laws with an empty `references` array are counted and reported separately (R2's never-codified class), not sampled. **(C) Fire-rate arm — the sample frame is not authored by this session.** Two strata: (C1) a deterministic uniform sample of section granules from the current `collection:USCODE` edition, taken by walking the full result set and keeping every k-th section-class granule (k chosen to yield ~150), the detector query `collection:PLAW congress:119 uscodecitation:"{t} U.S.C. {s}"` run per section, and each section also classified by the ground truth so the result is a 2×2 (detector fires / truth lists) rather than a bare rate; (C2) the sections real consumers are recorded requesting — 17 U.S.C. 107 and 42 U.S.C. 2210 (the manifest), 15 U.S.C. 260a and 261 (S11) — reported as a list, since n is too small for a rate. The `runs/` trace files in this directory are congressMCP-era and contain no uscode-mcp requests, so no larger consumer-authored sample exists. **(D) Latency arm (R14b).** Every detector-shaped query is timed end to end from this session's network position; report n, median, p95, max. Expected: median well under 100 ms of server time is NOT what this measures — it measures round trip including transit, so the honest comparison to the maintainer's threshold is the median with transit disclosed. Pre-committed disposition: median under ~100 ms → R14b's unconditional firing stands; materially above → back to the maintainer with the number. **(E) Instrument check for the general contract.** The 2024 edition is the only current edition, so `congress:119` is a valid proxy today but not a contract; the generic form is a date bound on `approveddate` (documented S4, never exercised). Measure `collection:PLAW approveddate:range(2025-01-07,2099-12-31)` against the `congress:119` public-law count; if they agree, the contract can be written on `approveddate` relative to the response's own `currentthrough`, which also makes the flag correct for `year`-selected historical editions. **(F) The note stratification is not measurable with this instrument — recorded now, not discovered later.** The probe of `PLAW-119publ21`'s summary shows `references` entries are bare section strings under a title; no entry distinguishes a note from section text. So ground truth cannot label a (law, section) pair as note-codified, and the preregistered note-vs-section split has no denominator. What survives: the server strips "note" before querying (O16), so the detector for a note citation IS the detector for its parent section, and the one measured note-heavy case (42 U.S.C. 2210, O17/O18) is reported per-law across every 119th law whose `references` list 2210. That is one section, not a stratum, and the observation will say so. **(G) The effective-date caveat is not measured here** — R14a declines to retrieve and parse enacting laws, so E16 measures whether a later law is *indexed against* a section, never whether it changed the section's text; nothing in the fire rate should be read as a rate of actual textual staleness.

E15 — RUN 2026-09-16, CLOSED, outcome O47 (`90-observations.md`; artifacts `runs/2026-09-16T003754Z/`, read by this session). Primary reading: one copy per inline result, the 2× falsifier not observed, O40's acceptance upgraded from inference to measurement. Second reading: the 100,000-char default is not delivered inline on claude-code 2.1.236 — the driver measures it with a count-tokens call and delivers a ~3 KB stand-in — while a 20,081-char window is delivered inline; the pre-committed "host limit confirmed" disposition applies: default lowered to 20,000 chars in `40-tools.md`, build order WO-4. Token count, measured from the post-fix harness run read directly (O47c): 37,193 tokens for the default-window count-tokens body, above the ~26k preregistered estimate. Original preregistration follows for the record.

E15 (preregistered 2026-09-02, maintainer-approved; from O40b and F3). **Question:** how many copies of a large tool result enter the consumer's request context, and does a 100,000-char default window get delivered inline at all? These are one measurement because one capture answers both. **Why it can't be settled otherwise:** `api-surface.jsonl` records `path`, `model`, `tool_names` and `body_keys` per request but never message bodies, so no artifact in a run says what reached the model; and a consumer's own report of its context is not evidence (S10 — the instrument reporting on itself). **Instrument (harness-side, mcp-e2e):** extend the api-surface capture with per-request body size and, where the driver already calls `/v1/messages/count_tokens` (observed at seq 3 in isolation/C/C1), the returned count — keys only, never bodies, so nothing new is published into artifacts. Run C1 in at least the floor and isolation cells. **Expected:** request context grows by ~1x the serialized envelope per tool result (~26k tokens for a 100k window), matching O40's accepted reading. **Falsifier:** growth of ~2x the envelope — then the duplication costs model context as well as wire, O40's acceptance is void, and the response shape gets reopened. **Second reading, from the same capture:** if the 208KB result never appears in a request body at full size, the F3 retreat is a host inline-delivery limit and the default `max_chars` is above it; if it appears in full, the retreat was consumer preference and F3 is a tuning question, not a threshold one. **Pre-committed dispositions,** so the result picks rather than reopens a debate: 2x context → response-shape work (the summary-plus-pointer option F1 parked); host limit confirmed → lower the default to below the measured threshold and say why in the contract; consumer preference → leave the default and close F3 as no-change.

## Previously preregistered — E11–E14, all resolved

E12 — CLOSED, confirmed 2026-09-02 (run 035906Z; disposition in `70-run-findings.md`): the banner led `text.content` in every truncated trace record, all three cells' C1 answers stated the true total (3,590,552) and none presented a window or the host-saved file as the whole law; the falsifier (a C1 failure WITH the banner present) did not occur. Original preregistration follows for the record. E12 (preregistered 2026-09-01, from finding F1; open as of run 023317Z — that C1 re-run PASSED but its trace shows no banner, so the change under test was absent and the pass counts as evidence of floor variability, not of E12; see `70-run-findings.md`). Change: the in-band truncation banner (`40-tools.md`). Expected: on the next run, C1-class behavior stops — the consumer states the true total and does not present a window or the host's saved tool-result file as the complete law. Falsifier: C1 fails again WITH the banner present at the head of `text.content` in the trace — then the banner is insufficient, the ruling gets revisited, and the finding escalates to a harder response-shape question (e.g. whether huge-payload success responses should default to a summary-plus-pointer shape instead of text).

E13 — RESOLVED at rung 0 (2026-09-01, run 024850Z; disposition in `70-run-findings.md`): the floor consumer located the needle in 9 calls via TOC-informed offset jumps plus an invented `packageid:` presence check (now measured, O30, and documented in the spec). No machinery built; reopens only on a real exhibited failure, at which point rung 1 is the preregistered next step. Original preregistration follows for the record. E13 (preregistered 2026-09-01). The question behind the maintainer's index question: does the PLAW package-only retrieval surface support interior content questions about huge laws, or does uscode-mcp need what congressMCP needed (a local search layer)? Instrument: prompt C3 (grounded needle: AUKUS in PLAW-118publ31, substantive cluster ~35% deep, TOC scent trail in window one). Expected at the floor: honest-incapability or heroic sequential paging (an over-work finding either way). Falsifier of the gap's urgency: the consumer reliably locates the material cheaply (TOC-informed jumps) — then existing tools suffice and no machinery is added. Disposition ladder, pinned before the run so the result picks a rung rather than relitigating: (1) a transient server-side within-law search — fetch the package text per request, return offset-anchored snippets that feed the existing start_char windowing; no corpus, no persistence, R1-compatible; (2) USLM-structure TOC/part extraction (USLM measured present for congresses 113+, O25); (3) a congressMCP-style persistent index, only if 1-2 are measured insufficient. Which rung, if any, is the maintainer's call once the measurement is in.

E11 — arm (a) RUN 2026-09-01 (O31): CONFIRMED. Under ChatGPT auth, `tools.web_search=false` plus a read-only sandbox does not disable the web-search tool (it ran seven times and proxied via r.jina.ai), and the locked run fabricated the fetch target. R10's mandate is now active: the harness MUST refuse cross-vendor cells under ChatGPT auth. Arm (c) RUN 2026-09-01 (O32), prompted by the maintainer pointing at congressMCP's custom-provider approach: the facility works under ChatGPT auth at the routing level, the ChatGPT token is never sent to a custom provider (so a filtering proxy needs its own API-key backend — separate billing, now measured), and the wire capture shows codex offers `web_search` plus the user's ambient MCP servers and plugins to any provider — so cross-vendor cells need BOTH a filtering proxy (or a knob that works) AND an isolated CODEX_HOME. Arm (b) RUN 2026-09-01 (O33): `tools.web_search=false` is dead under API-key auth too — the knob was never the fix. Final mechanism (maintainer-supplied from congressMCP, reproduced here as O34): the noweb custom provider — same api.openai.com backend, no `supports_standalone_web_search` declaration, so the CLI never registers the web tool; no proxy needed. E11 is RESOLVED; the binding gate is R10's second amendment in `96-rulings.md` (noweb provider + isolated CODEX_HOME + per-run effect verification). All findings pinned to codex-cli 0.147.0; re-measure on upgrade. Original claim from congressMCP work: ChatGPT-authenticated Codex cannot have web fetching disabled; the maintainer suspects a way to do it was found only after switching to API auth. Measurement: attempt to fully disable web/network tools under (a) ChatGPT auth and (b) API-key auth, verifying with a prompt that can only be answered by fetching (expected: fetch succeeds or tool remains available under (a); disabled verifiably under (b)). Expected: the claim holds → the harness MUST refuse cross-vendor cells without API-key auth (R10). Falsifier: a ChatGPT-auth configuration that verifiably disables fetching → the mandate relaxes to requiring that configuration.

E14 — CLOSED, confirmed 2026-09-02 (run 035906Z; disposition in `70-run-findings.md`): every cell reached for `find` immediately on both locating tasks, blind offset guessing appears in none of the nine traces, and the non-note control (C3) fell from the 9-call pre-`find` baseline to 4 calls at the floor. The deferred notes-only mode is dead — E14 was its reopening condition and resolved against it. Original preregistration follows for the record. E14 (preregistered 2026-09-01, gates R12's deferred notes-only mode). After R12a/b ship: the S8 task — locate RECA content inside 42 U.S.C. 2210's payload — should take one locating call (`find` or `structure`) plus one window, no blind offset guessing. Instrument: a manifest prompt (C4, to be added grounded once the facility exists) asking for specific note content deep in a large section, plus at least one big NON-note-heavy section per S8's caution against note-specific machinery. Expected: consumers use the facility and stop guessing. Falsifier: blind windowing persists with the facility documented — then the facility's discoverability (tool description wording) is the finding, and the deferred notes-only mode gets reconsidered.

## Run — see observations

E5's remainder ran 2026-08-29 (O22): `X-Api-Key` header auth confirmed with a 401 no-credential control — and promoted from alternative to the only permitted transport by R7.

E9 ran 2026-08-29 (protocol in commit a455a66, outcome O21): 25/33 recall, recency hypothesis falsified — the gap is structural. Q6(b) is now measured and awaits a maintainer ruling on whether a v2 references-array-based join gets designed.

## Preregistered experiments

None pending. E1–E10 have all run; outcomes are O10–O26 (see `90-observations.md`). New experiments get preregistered here before running, per convention.

Closed 2026-08-30: E3 — `resultLevel:"package"` zero-hits granule-field queries, a hazard not a tool (O23). E4a — PLAW USLM boundary at the 113th Congress (O25). E6 — bulkdata has no USCODE repository; GovInfo's USCODE format story is complete as measured (O26). E10 — direct appendix citation forms exist; the appendix redirect upgraded to direct resolution with the redirect demoted to zero-hit fallback (O24).
