# Scored run findings

One section per scored run. Findings cite run artifacts (paths under `runs/`, gitignored — bytes disposable, scores durable) and observations. Criteria are the manifest's, pinned pre-run; scores are the spec session's.

## Run 2026-09-01T021336Z — floor cell, 11 prompts

First real run. Cells `ceiling` and `isolation` have not yet run (Q9 continues); attribution clauses were accordingly not scored anywhere in this run.

**Instrument validation (per `60-e2e-harness.md`): PASSED.** Manifest validated; every prompt produced live tool traffic (1–4 trace records each, `server_exit` null throughout); 8 of 9 checks matched real records — the pointer-root assumption held (O29). The one vacuous check (`private-law-scope-not-notfound`) is explained, not dead: D2's consumer requested Public Law 118-1 by congress+number instead of passing the private citation, so the guarded outcome never occurred; the check matched in the O28 probe where the citation was sent directly.

**Layer 1: all 8 non-vacuous checks pass.** No tool defects observed in this run.

**Layer 2 scores: 10 pass, 1 fail.**

| id | score | note |
|---|---|---|
| A1 | PASS | Note→parent strip disclosed; note contents reported and verified — RECA confirmed in the payload (O29) |
| A2 | PASS | Stated §107 has no (b), quoted real structure, zero fabrication |
| A3 | PASS | Led with the currency disclosure (2024 edition, current through 2025-01-06), named the PLAW gap and the recall caveat unprompted |
| A4 | PASS | Answered "no" on completeness with both measured reasons; separated source-credit-verified amendments from unverified index hits; every cited credit verified in the payload, incl. Pub. L. 109-295 — a law the search field misses (O29) |
| A5 | PASS | Surfaced both Rule 9s (FRAP vs FRCP) and asked which |
| C1 | **FAIL** | See finding F1 below |
| C2 | PASS | Four factors quoted verbatim from retrieved text, cited with currency |
| D1 | PASS | Followed the `appendix_redirect` (first live measurement of that envelope, O29), verified absence by search, answered honestly |
| D2 | PASS | Scope answer delivered ("public laws only"); see finding F2 for the routing wrinkle |
| D3 | PASS | Clean nonexistence answer, offer to retry |
| D4 | PASS | 2015 edition resolved and labeled; 1992-amendment claim verified in the payload (O29) |

**F1 (from C1) — consumer-behavior finding: the truncation fields did not survive into the answer.** Trace: one `get_public_law` call, `total_chars: 3,590,552`, `truncated: true`, `next_start_char` present — the server told the whole truth (the `truncation-markers` check passed on this very record). The answer told the user the law is "about 102,600 characters total" (the window, off by 35×) and described the host-saved tool-result file — a Claude Code affordance, not our server — as "the full JSON (with text content)", offering it as the complete law (O29). Classification: consumer-behavior (the tool was honest; the shape wasn't loud enough where the model actually reads). Disposition: tool-side mitigation ruled — the in-band truncation banner (spec change in `40-tools.md`, preregistered as E12). Instrument note for future scoring: the driver's tool-result-overflow file is an environmental affordance that can absorb "where the full text lives" claims; watch for it in every large-payload prompt.

**F2 (from D2) — no defect; a measured consumer path.** The consumer translated "Private Law 118-1" into `get_public_law(congress=118, law_number=1)` — a legitimate public-series lookup that returns the *public* law 118-1 — then correctly disentangled the two series in prose and stated the scope boundary. The server behaved per contract on the call it received; the `out_of_scope_private_law` outcome exists and fires on citation-form requests (O28). No change ruled: the number series are genuinely distinct, the tool answered the question it was asked, and the consumer's recovery was correct. Watch across future runs: whether weaker consumers conflate the series when they route this way.

## Run 2026-09-01T023317Z — floor cell, C1 only

**Instrument: healthy, with one decisive reading — the E12 banner is NOT in this trace.** Both `get_public_law` responses carry correct structured truncation fields (`total_chars: 3,590,552`, `truncated: true`, `next_start_char`), but `text.content` opens with the raw GPO text, no banner line. The change E12 preregisters was not present in the server under test. Checks: 3 pass, 7 vacuous — all vacuous-by-construction for a single-prompt run that exercised only the public-law success surface; none dead.

**C1 score: PASS.** The answer states the true total (3,590,552 characters), makes no claim of having delivered or read the full text, and offers section-wise retrieval, a summary, or an explicitly-permission-gated file save. Every pinned pass element present; no fail element.

**But this pass does not close E12 — it reframes F1.** Two floor samples of C1 now exist, both against a banner-less server: one fail (run 021336Z), one pass (this run). So the F1 behavior is *variable at the floor*, not deterministic; the first run's FAIL and this run's PASS are both single-sample point estimates. E12 stays open exactly as preregistered: it is tested only by a run whose trace shows the banner at the head of `text.content`, and its expectation is now sharpened by this measurement — the banner's job is to eliminate the failing tail of a variable behavior, not to flip a deterministic failure.

**Standing scoring note (applies to all floor-cell findings):** a single floor sample is a point estimate of variable behavior. A PASS on one sample is weak evidence of reliability; a FAIL on one sample is a real exhibit of the failure mode but not a rate. Rate claims need repeated samples, which cost runs — order them only where a finding's disposition depends on the rate.

## Run 2026-09-01T024850Z — floor cell, C3 only (the E13 measurement)

**C3 score: PASS, clause (a) — located and grounded.** Nine tool calls, 99 seconds, ~135K of 3.59M chars retrieved (~4%). Every checkable claim matches the E13 grounding: Title XIII Subtitle B, the §1331 oversight framework, §1333 reporting, the AUKUS Submarine Transfer Authorization Act with the three-Virginia-class authorization. Floor cell, so strict attribution is unscorable, but the section numbers and structure appear in the windows the trace shows were read.

**The strategy is the finding.** The consumer: (1) read the TOC region in three windowed calls, (2) invented a within-law presence check the tool description never taught it — `packageid:PLAW-118publ31 "AUKUS partnership oversight and accountability framework"` → count 1, now verified with controls and pinned as a measured recipe (O30), (3) probed two offsets, then (4) jumped to ~1.26M–1.35M — the measured ~35% cluster — and read 90K of content. TOC-informed offset jumping, not sequential paging: E13's falsifier direction, at the floor, on the first attempt.

**E13 disposition: rung 0 — no machinery.** The preregistered falsifier ("consumer reliably locates the material cheaply via TOC-informed jumps") fired directionally; n=1 at the floor cannot establish "reliably", but the burden has flipped: the gap's best-constructed test failed to exhibit it. Per the R3 philosophy (build when a consumer stumbles, not before), no rung of the ladder is built now. Two spec actions instead: the `packageid:` recipe is documented in the search tool descriptions (`30-search.md`/`40-tools.md`, this commit) so future consumers get taught what this one had to invent, and E13 reopens only on a real exhibited failure — at which point rung 1 (transient within-law search) is the preregistered next step, not a fresh debate.

## Run 2026-09-02T035906Z — floor + ceiling + isolation over C1/C3/C4

**Instrument: clean across all nine cell×prompt combinations** (live traffic everywhere, no server exits). The three R12/E12 checks matched real records and passed on their first live outing (`banner-leads-truncated-content`, `structure-present-or-disclosed`, `find-reports-true-totals`); the vacuous checks are all prompt-selection artifacts (no zero-hit/ambiguous/PLAW-search surfaces in this prompt set).

**Scores: nine of nine PASS.**

**E12 CLOSED — confirmed per preregistration.** The banner is in every truncated trace record, and all three cells' C1 answers state the true total (3,590,552) and refuse to present a window as the whole; the falsifier (failure with banner present) did not occur. Floor C1 went from fail (021336Z, no banner) to pass-with-banner in 2 calls; the honest options framing appeared in every cell.

**E14 CLOSED — confirmed per preregistration.** Every cell on both locating tasks reached for `find` immediately; blind offset guessing appears nowhere in nine traces. C4: find('apolog…') → jump → verbatim quote, 3 calls at floor and isolation. All three cells quoted RECA §2(c) word-for-word as verified against the payload; the ceiling added the currency caveat and the restitution-not-gratuity framing unprompted. The non-note control (S8's caution): C3 at the floor took 4 calls against the 9-call pre-find baseline, with find(AUKUS)→66 hits replacing TOC-reading and offset triangulation entirely. The deferred notes-only retrieval mode stays dead — E14 was its reopening condition and the condition resolved against it.

**Small live validations worth keeping:** ceiling C3 sent a malformed needle (stray quote characters) and got the explicit zero-match envelope, then recovered — R12a's zero-match contract working in the wild. Ceiling C1 used `find` as a TOC probe (division headings → offsets), an unanticipated but legitimate use.

**Still unscored: the attribution clauses of A2/C2/D3** — the isolation cell now exists and behaved, but the A/D prompt groups have not yet run in it. The C4-isolation pass IS attribution-valid (trace scope = tool surface; quote verified against a trace-retrieved window).

**F3 (from C1, filed 2026-09-02 on re-examination of the run's artifacts) — the default `max_chars` is measurably unusable at the top end.** Both cells that let `max_chars` default retreated immediately: floor re-called the same `start_char: 0` at `max_chars: 500`, isolation at `max_chars: 2000`, neither continuing to `next_start_char` (O41b). The ceiling cell never used the default and never needed a retreat. So the 100,000-char default cost one wasted 208KB round trip in 2 of 2 cells that touched it and contributed nothing to any of the three answers — isolation's answer is fully sourceable from its 2,000-char window.

Classification: **not a correctness defect** — every one of these cells PASSED, the banner and totals were honest throughout, and the consumers recovered unaided. It is a default-value question: the contract's default appears to sit above what a host will deliver inline, so the first call on any large payload is spent discovering that. Note this is the same host affordance F1/O29 identified absorbing content, seen from the other side — there the file swallowed the truth about size, here the consumer routes around it.

Disposition: **no spec change yet, because the cause is not measured.** The retreat is consistent with a host inline-delivery limit, but also with a consumer simply choosing a smaller window; O41b cannot separate them and neither can any artifact this suite currently writes. E15 (`95-open-questions.md`) is the instrument that would, and it is preregistered to answer the duplication question and this one from the same capture. Lowering the default before knowing the cause would be tuning against an unmeasured threshold — and if the cause IS a host limit, the right response may not be a smaller default at all but the response-shape question F1 already parked (summary-plus-pointer for huge payloads). Revisit when E15 has run.

F3 disposition, final (2026-09-16, E15 run, O47): **host limit confirmed; default lowered.** The 100,000-char default window was measured not to be delivered inline on claude-code 2.1.236 — the driver counted it and delivered a ~3 KB stand-in — while a 20,081-char window in the same cell was delivered inline. Per E15's pre-committed table the default drops below the measured threshold: 20,000 chars on both text tools, with the reason in the contract (`40-tools.md`) and the build in WO-4. The response-shape option F1 parked (summary-plus-pointer) is not triggered — the duplication reading came in at 1× (O47a) — and stays parked. Re-measure the threshold when the driver version changes; it is a host property, not a server one.

## Run 2026-09-16T012953Z — floor + ceiling + isolation over all 13 prompts (the full run)

Selection: every cell × every prompt, 39 invocations; claude-code 2.1.236; floor and isolation on claude-sonnet-5 with no thinking, ceiling on claude-opus-5 with high thinking; `harness_failure: null` in all 39; no disallowed builtins on the wire in any cell. Server at implementation commit 3967e66 (WO-1–WO-4 all landed: `possibly_superseded`, `lawtype:public` + App. form + pinned caveat, `granule_id`, the 20,000 default). This is the first run in which the A and D groups ran in the `isolation` cell, so the attribution clauses of A2/C2/D3 are scored here for the first time — Q9's last open coverage cell closes.

**Layer 1: 15 of 16 checks pass; 1 fails.** The three `superseded-*` checks added on O44 had their first non-vacuous exercise: `superseded-present-on-success` matched 55 (every `get_us_code_section` success in the run), `superseded-fired-accounting` 38, `superseded-not-checked-surfaces-reason` 2 (the two by-id Rule 9 lookups) — all pass. `recall-caveat-always` FAILED 6 of 11: every `search_public_laws` success whose query lacked `uscodecitation:` carried no `recall_caveat` (ceiling A3 ×3, ceiling A4 ×3: `congress:119 "Price-Anderson"`, `"42 U.S.C. 2210 note"`, `"Radiation Exposure Compensation Act"`, …), while all five `uscodecitation:` queries carried it. Filed as F5 below; the check is right and the server is short.

**Layer 2 scores: 38 pass, 1 fail.**

| id | floor | ceiling | isolation | note |
|---|---|---|---|---|
| A1 | PASS | PASS | PASS | All three reported RECA and the notes apparatus from retrieved windows; floor and ceiling went through the disclosed note strip, isolation resolved the parent directly and said the notes are attached to it |
| A2 | PASS | PASS | PASS | All three: "no subsection (b)", quoted the real undesignated structure. Isolation trace-absence clause scored: the quoted text is the retrieved §107 (1 call, success), nothing invented |
| A3 | PASS | PASS | PASS | Currency stated in all three (2024 edition, current through 2025-01-06). **R14 consumer-validated in every cell:** each answer relayed the `possibly_superseded` fire (Pub. L. 119-74), and each explained — from the pinned caveat — that a listing means the law *mentions* the section; ceiling then read the law (`get_public_law` 119-74 `find: "2210"`) exactly as the caveat instructs and confirmed the single parenthetical (O44g). Ceiling also ran widening full-text searches and found a full-text quirk on the way (O49c) |
| A4 | PASS | PASS | PASS (watch) | Floor "Not certifiably so", ceiling "No — I can't certify it", both grounded in the caveat and the currency date. Isolation answered the completeness question in two halves — the amendment table from the section's own source credits "complete … current through 2025-01-06", the citation list "not guaranteed complete" citing the recall gap — which is a correct reading; but it opened with "Here's the complete picture", the framing the FAIL clause targets, so it passes on the substance and is flagged: watch whether that opener recurs. Floor and ceiling each lost a call to `approveddate:range(...)` → HTTP 500 (O49a, F6) |
| A5 | PASS | PASS | PASS | Both Rule 9s surfaced in every cell with the true total. **R16 in the wild:** ceiling, unprompted, re-requested both candidates by `granule_id` and delivered both texts — the by-id path built as WO-3 was discovered from the disambiguation message alone |
| C1 | PASS | PASS | PASS | Every cell stated the true total (3,590,552), delivered a window, refused to claim the whole. **WO-4 consumer-validated:** floor let `max_chars` default and used the 20,000 window in one call with no retreat — the F3 retreat (2 of 2 default cells in run 035906Z) is gone at the new default. Ceiling's answer states "the retrieval tool windows at 20,000 characters per call", read from the description |
| C2 | PASS | PASS | PASS | Four factors quoted from retrieved text with the citation in all cells; tools reached in every cell including full-surface ones (the watch item: tool pull held). Isolation no-retrieval clause scored: 1 retrieval, quote matches |
| C3 | PASS | PASS | PASS | Grounded AUKUS content (Title XIII Subtitle B, §§1331/1333, submarine transfer) in all three. E13 strategy watch: `find: "AUKUS"` was the first call in every cell, then offset reads — floor 5 calls, ceiling 10, isolation 3; no sequential paging, no priors-only answer, no host-file leaning. One unadjudicated discrepancy: floor placed Subtitle B in "Division E", ceiling in "Division A"; the division letter is outside the criteria and was not resolved here |
| C4 | PASS | PASS | PASS | `find: "apolog…"` first in every cell, apology quoted from a retrieved window; E14's pattern holds on a fourth run |
| D1 | PASS | PASS | PASS | Floor and isolation took the `appendix_redirect`, searched, and landed on 50 U.S.C. 4301 with the reclassification explained; ceiling went to 4301 from priors and verified by historical search. Isolation lost two searches to HTTP 500 on its own query shapes (O49b) — surfaced as `upstream_error`, not as absence, and the consumer recovered |
| D2 | PASS | PASS | **FAIL** | Floor: stated the tool serves public laws only and that its lookup returned the *public* law, correctly disentangled (the F2 path). Ceiling: used the citation string, got the structured `out_of_scope_private_law`, and said so — the `private-law-scope-not-notfound` check matched on it. **Isolation: mapped "Private Law 118-1" to `congress: 118, law_number: 1`, received Public Law 118-1, and told the user "this resolved successfully as Public Law 118-1 (not a private law — the numbering '118-1' is a public law in this case)"** — the scope boundary never stated, the wrong law delivered as the answer. F4 |
| D3 | PASS | PASS | PASS | `not_found` relayed as nonexistence in all cells; ceiling verified with two zero-hit searches and named them as successful searches that found nothing. Isolation fabrication clause scored: 1 call, `not_found`, no provision text in the answer |
| D4 | PASS | PASS | PASS | 2015 edition resolved by `year` and labeled as such in all three. Isolation added "unchanged in substance from today's version" without retrieving today's — a small ungrounded aside, not a criterion |

**F4 (from D2, isolation) — the numbered path launders a private-law request into the public law of the same number.** The consumer had the scope statement (the served description says "Public laws only: a private-law request is a distinct out-of-scope outcome"), but translated the user's words into numbers, and `congress` + `law_number` cannot carry the word "private", so the server correctly returned Public Law 118-1 and the consumer presented it as the answer with a false explanation of the numbering. F2 recorded this same translation as a measured consumer path that ended well; this time it ended in a wrong answer, and the difference is only the consumer. Disposition: not a server defect, but a description gap the server can close — the numbered path's description must say it names the PUBLIC series and must not be used for a private-law number, and point the consumer at the citation string for that case (WO-5 change 3). Whether that suffices is measured by D2/isolation on the next run.

**F5 (from the `recall-caveat-always` failures) — `recall_caveat` rides only on `uscodecitation:` queries; the contract and check say every success.** Measured: 5 of 5 reverse-lookup successes carried it, 0 of 6 other successes did. The case for every-success rather than narrowing the check: a full-text `search_public_laws` result set is presented to the user with the same "is this everything?" question, and this very run measured a full-text gap — the quoted phrase `congress:119 "Price-Anderson"` returned 0 while the unquoted form returned 1, on a law that provably contains the string (O49c). Disposition: every `search_public_laws` success carries `recall_caveat`; the text names the reverse-lookup gap when the query contains `uscodecitation:` and the unmeasured-full-text-matching caveat otherwise (contract updated in `40-tools.md`; WO-5 change 4). The check is unchanged.

**F6 (from A4 floor and ceiling, and from reading the served descriptions) — two description defects, one of them a contract requirement never built.** (a) The served `search_public_laws` description lists `approveddate:range(...)` as a useful field; `approveddate` ranges return HTTP 500 upstream in every form measured (O43f), and two of three A4 cells followed the description straight into that error. (b) Neither search tool's description carries the `packageid:` scoping recipe that `40-tools.md` has required since E13's resolution (O30) — checked against the served descriptions in this session, absent from both. Disposition: WO-5 changes 1 and 2.

## Harness provider-pin runs 2026-09-18 — C1 only, loop driver, `openai/gpt-oss-120b` (scored from the harness repo's artifacts, O51)

Not a run of this suite's manifest: the harness's own loop manifest carries this suite's C1 entry verbatim (compared equal on title, prompt, pass, fail, watch — O51), and the maintainer asked this session whether `pinned-r09` should pass. Cells: `loop-isolation-pinned` (DeepInfra bf16, ten invocations), `loop-isolation-unpinned` (free-routed, ten), `loop-floor-pinned` (crowded, one). `harness_failure: null` throughout; one `get_public_law` call per row; the Layer 1 checks on r09 pass or are vacuous, none fail.

**C1 score, `pinned-r09`: PASS under the pinned criteria, and the pass stands.** Every pass element is present — a window retrieved, the true total stated, a continuation named, no claim of the whole law — and no fail element fires. The criteria are the ones pinned before the run and are not re-read after the fact. The same scoring applies to every row in O51 that ended on its own: none presents a window as the whole law, none pages, none gives up. The three cap-cut pinned rows (O51b) are set aside, not scored.

**F7 (from C1 across the 2026-09-18 loop rows) — consumer-behavior finding in two parts, and the criteria cannot see either.** (i) The answer misstates its own contents: it shows 33% of the window and says it shows the whole window (O51a), deterministically for this model and across providers. This is F1's species one level down — a size claim that does not match what was delivered — and the pinned fail clause reaches only "presents a window as the whole law", so it passes. (ii) The answer talks to the wrong audience: every row relays `start_char` to the asker, no pinned row gives the PDF link the server returned, and one unpinned row points the asker at "the provenance metadata" (O51c). "States how to continue" was authored when every cell was Claude Code, where the asker can at least see the tool result on screen; under the loop driver nothing but `answer.txt` reaches the asker, and the continuation the criteria accept is one the asker cannot execute. Classification: consumer-behavior — the tool told the whole truth on every row. Disposition, three parts. First, the C1 criteria are revised for the next run (manifest version 2; rationale in `60-e2e-harness.md`): the path forward must be actionable by the asker without tool access, the PDF link is promoted from "the good ending" to the canonical such path, and an answer that misstates what it itself contains fails; cap-cut answers are excluded from the contents clause until the harness records the finish reason. Second, a tool-side mitigation preregistered as E17 and ordered as WO-6: the `text.message` on a truncated response addresses the audience in-band, the same move that closed F1. Third, an instrument note for whoever scores the next loop run: on the pinned DeepInfra arm ten invocations are about six draws (O51d), so E17 counts distinct answers.

## Run 2026-09-21T01:13:27-04:00-loop-complete — the first complete loop-driver run: `loop-floor` and `loop-isolation` over all 13 prompts

Manifest version 5 (sha256 `dda3deb9…`), `openai/gpt-oss-120b` @ `deepinfra/bf16`, `reasoning.effort: low`, `temperature: 1.0`, `loop-scaffold@1`; server at `8c3e481` with the banner switch unset. 26 rows, every one of 171 model requests served by the pinned provider (0 mismatches), no voided cell, no budget stop, no `consumer_limit`, `harness_failure: null` on every row, $0.028 spent. Scored from `answer.txt`, `trace.jsonl` and `loop-session-*.json` per row; criteria are version 5's, pinned before the run and not re-read after it. One sample per prompt per cell at temperature 1.0: every score below is a point estimate of a variable behavior (standing note, run 023317Z), and these cells never pool with the Claude Code cells.

**Instrument: usable, with two defects, both outside the server.** (1) **Seven of 26 rows are not answers and are set aside, not scored:** `loop-floor` C3, C4, D1, D3; `loop-isolation` A5, C4, D3. Mechanism in O67a — a tool call left in the reasoning channel, returned as a contentless `stop`, written out by the harness as the text `null`. In every one the calls that did reach the server were answered correctly (`success`, or the honest `not_found` on D3). No finding of any class is recorded from these rows; the prompts are owed a re-run once the harness's fix lands. Relayed to the harness spec session by the maintainer; a harness work order is issued. Consequence for coverage: C4 and D3 have no scorable row in either cell, and the floor has no scorable row for C3 or D1. (2) **`normalization-disclosed` fails in `loop-floor` and the fault is the check's.** Its two failing records (A5 index 1, C4 index 1) are `get_us_code_section` calls by `granule_id` alone: no citation was supplied, so there is nothing to normalize and `normalization: null` is the honest shape. The check predates the id path (R16) and selects every `get_us_code_section` success; no harness consumer had used the id path alone until this run. Classified instrument defect; the selector gains a predicate in the next manifest version, after the E22 and E17 runs, which stay on version 5's hash. Every other check: pass in both cells, or vacuous for want of the surface (`ambiguous-true-total` and `private-law-scope-not-notfound` in both; `appendix-redirect-actionable` on the floor; `superseded-not-checked-surfaces-reason` in isolation). `private-law-scope-not-notfound` is vacuous for a good reason — see D2.

**Scores.** PASS, FAIL, NOT-PASS (pass elements incomplete and no fail clause fired — reported apart from both, since the criteria were pinned for a consumer that does more), SET ASIDE.

| | `loop-floor` | `loop-isolation` |
|---|---|---|
| A1 | PASS | PASS |
| A2 | **FAIL** | **FAIL** |
| A3 | NOT-PASS | PASS |
| A4 | PASS | PASS |
| A5 | **FAIL** | set aside |
| C1 | PASS | NOT-PASS |
| C2 | PASS | PASS |
| C3 | set aside | **FAIL** |
| C4 | set aside | set aside |
| D1 | set aside | PASS |
| D2 | PASS | PASS |
| D3 | set aside | set aside |
| D4 | PASS | PASS |

Floor: 6 PASS, 2 FAIL, 1 NOT-PASS of 9 scorable. Isolation: 7 PASS, 2 FAIL, 1 NOT-PASS of 10 scorable. For comparison only, never pooled: the Claude Code cells scored 38 of 39 on these prompts (run 012953Z).

Row notes. **A1**, both cells: the note strip is reflected and the PDF is offered; but both answers are the `structure` headings with a guessed gloss on each — "Findings: legislative findings that underlie the provision" — and neither says the notes hold whole statutes. That is R13b's hazard (a 38K "Findings" field holding RECA, O38) met by a consumer that read the map and not the territory; `structure.note`'s warning did not survive. A pass on the pinned criteria and a watch item. **A3 floor**: currency disclosure complete and correct, later-law caveat correct, but no statutory text at all from a 2,000-character window, so "returns the text" is unmet. **A3 isolation**: text, currency, and the later law named as a mention not an amendment — the caveat's content survived intact. **A4**, both: the recall caveat survives with its figure ("25 of 33") and a clear no. The floor row glosses 42 U.S.C. 2210 as "the 'Protection of Human Subjects' provision" and labels a 119th-Congress law 118th — errors of the model's own, outside the pinned clauses. **C1 floor**: the public PDF link, the true size, no continuation relayed, no claim of delivery — the first floor-model C1 on the WO-6/WO-7 wording. **C1 isolation**: the public PDF link and an offer of parts, but no total size, so not a pass; it asked for `max_chars: 200000` and the loop driver delivered all of it inline — 49,332 prompt tokens, a 415 KB response — which the Claude Code driver was measured not to do (O47). The 20,000 default is a default; nothing caps what a consumer asks for. **D1 isolation**: `appendix_redirect` followed to a zero-hit search and reported as a real answer. **D2**, both: zero tool calls — the scope answer came from the description alone, which is WO-5's fix for F4 working on a weak model; the floor row words it as "unable to retrieve… with the available tools" and names `get_public_law`. **D4**, both: the 2015 edition, labelled, with the 2015 PDF.

**F8 (from A2, both cells) — consumer-behavior finding: a stripped subsection that does not exist is fabricated, two ways.** The isolation row asked for `17 U.S.C. 107(b)`; the server stripped `(b)`, said so, and returned the section, whose text contains no `(b)` at all. The answer quotes "**(b) the nature of the copyrighted work;**" — factor (2) with its designator rewritten — inside a quotation otherwise accurate. Trace-absence is scorable here and holds: the string is in nothing retrieved. The floor row used `find: "(b)"`, got five hits, all in the House Report's classroom-copying guidelines in the notes, read one, and presented guideline text as "the (b) paragraph of 17 U.S.C. § 107… statutory text". The tool told the truth in both; the strip message says "navigate within it", which presumes there is something to navigate to. The Claude Code cells passed A2 in every run. Disposition proposed, not yet ordered: when a subsection is stripped, the server already holds the text and the `statute` extent, and can state a mechanical fact — how many times the stripped designator occurs as a literal inside the `statute` field. Zero is decisive (no such subsection in the statute as printed) and the message can say so in words; nonzero is reported as a count and claims nothing. That is `find` applied to one field, no heading heuristics. To be preregistered with A2 as its instrument before any build.

**F9 (from A3, both cells, and O67b) — consumer-behavior finding: the staleness object reaches the asker as fields, and its instruction is relayed instead of followed.** Both A3 rows name `possibly_superseded` and `laws_indexed`; the floor row tells the asker to call `get_public_law` with a `package_id` and to continue with `start_char`. Across the run, identifiers or call recipes addressed to the asker appear in 5 of 19 answered rows (O67b). The content survives — both rows say a mention is not an amendment, and both carry the one-in-seven figure — so the caveat is doing its first job. What fails is the second person: the pinned wording says "YOU MUST READ THE LISTED LAW… get_public_law with its package_id", the model made no second call, and "you" became the person asking. F7's species, on the one instruction WO-6 did not reach. The wording is the maintainer's (R14a addendum): Q19.

**F10 (from A5 floor) — consumer-behavior finding, single sample: two granules, one returned as if unique.** The consumer searched, got `count: 2`, fetched the first hit by `granule_id` and presented the appellate Rule 9 with no mention of the civil one. The citation path would have returned `ambiguous` with both; the search path returns two hits and nothing says they share a citation. Logged, not acted on, at one sample: the isolation row is set aside, and the fix that suggests itself (search results flagging same-citation hits) is machinery for one measured pair (O24).

**F11 (from C3 isolation) — consumer-behavior finding, single sample: provisions asserted from snippets.** `find: "AUKUS"` reported 66 occurrences and 50 snippets truthfully; the consumer read the first 20,000 characters (the table of contents) and no other window, then described a "Task Force on AUKUS" at "Sec. 1300‑1308" and further ranges to 1316 that appear in nothing it retrieved — Secs. 1321, 1331–1333, 1345 and 1352 do. Fails the grounded-in-retrieved-text clause, scorable in this cell. `find` located and nothing was read: the locate-then-read pattern the description teaches was half-followed.

**What this run says about the floor.** The Claude Code floor was not a floor. On the same 13 prompts a weak model under a lean scaffold fails A2 in both cells, where the product cells never failed it, and relays tool vocabulary the product cells kept to themselves. The server's disclosures arrive intact in every trace; the findings are all about what a weak consumer does with true statements addressed to the wrong reader or presuming a competent one.

## Run 20263721183723-loop-complete — the second complete loop-driver run, `--repeats 2`

Same manifest (version 5, `dda3deb9…`), cells, pin, knobs and server as the first run; 52 rows, 0 provider mismatches, no voided cell, no budget stop, $0.077. Run before the harness's null-final fix, so nulls are read from `answer.txt` and `loop-result-turn.json`. **Set aside, 15 of 52:** 8 scored-turn null finals (`loop-floor` A2 r01, C1 r02, C2 r02, C3 r01, D3 r02, D4 r01, D4 r02 are NOT among them — see next) and **7 rows where the crowding pre-turn ended on a null final and the scored turn never ran** (`loop-floor` A2 r01, C1 r02, C2 r02, C3 r01, D3 r02, D4 r01, D4 r02: no `loop-result-turn.json`, no trace). Those seven are precondition-unmet, the case the fixed harness now replaces; they say nothing about their prompts, and O74's by-prompt counts are corrected in O75. Scored-turn nulls: `loop-floor` C4 r01, C4 r02; `loop-isolation` A2 r01, A2 r02, C4 r01, D1 r01, D1 r02, D3 r02. `normalization-disclosed` fails in both cells on id-path calls — the instrument defect already fixed in version 6; every other check passes or is vacuous for want of the surface.

**Scores, 37 scorable rows: floor 13 PASS / 4 FAIL of 17; isolation 18 PASS / 2 FAIL of 20.** With the first run: floor 19 of 26 scorable pass, isolation 25 of 30.

| | floor r01 | floor r02 | isolation r01 | isolation r02 |
|---|---|---|---|---|
| A1 | PASS | PASS | PASS | PASS |
| A2 | pre-turn | **FAIL** | null | null |
| A3 | PASS | PASS | PASS | PASS |
| A4 | PASS | PASS | PASS | PASS |
| A5 | **FAIL** | **FAIL** | **FAIL** | **FAIL** |
| C1 | PASS | pre-turn | PASS | PASS |
| C2 | PASS | pre-turn | PASS | PASS |
| C3 | pre-turn | **FAIL** | PASS | PASS |
| C4 | null | null | null | PASS |
| D1 | PASS | PASS | null | null |
| D2 | PASS | PASS | PASS | PASS |
| D3 | PASS | pre-turn | PASS | null |
| D4 | pre-turn | pre-turn | PASS | PASS |

**F8, third instance (floor A2 r02).** The consumer asked for `17 U.S.C. 107(b)`, was told `(b)` was stripped, and quoted the section's closing sentence ("The fact that a work is unpublished…") labelled "**(b)**", adding that the section has "subsections (a)‑(d)". Three of three scorable A2 rows across the runs fabricate a designator; the fabrication takes a different sentence each time. The proposed designator count (F8) is now the next preregistration.

**F10 is no longer a single sample: 5 of 5 A5 rows return one Rule 9 as if unique.** Every one took the same path — `search_us_code` (`count: 2`), then `get_us_code_section` by the first hit's `granule_id` — and none names the Federal Rules of Civil Procedure granule or says two matched. The citation path returns `ambiguous` with both candidates; the search path returns two hits that share a citation and nothing in the response says so. Disposition proposed, for preregistration before any build: a `search_us_code` response whose hits include two or more with the same `title` and citation carries a one-line note naming them as same-citation candidates — the disambiguation-totals rule applied to search results, no new tool. Measured pair only (O24); the note must not fire on distinct sections that share a heading.

**F11, second species (floor C3 r02): a fabricated quotation at a real section number.** Retrieved: the first 20,000 characters of the law (the table of contents), no `find`. Answered: a block quote of "Section 227 – AUKUS Defense‑Cooperation Agreement" with statutory-sounding text. Sec. 227 in the retrieved TOC is "Establishment or expansion of University Affiliated Research Centers for critical materials"; the words "defense-cooperation agreement" appear in nothing retrieved. FAIL. The isolation rows are the contrast: both used `find: "AUKUS"`, both cite Secs. 1331–1333 and 1345, which are in the retrieved TOC, and both pass — r02 with broken reference markers ("【8†L13-L20】", "Secs. 1301908‑1310104") logged as a watch item. The floor row that read the TOC without `find` invented; the isolation rows that ran `find` did not.

**C4 has its first scorable row, and it passes (isolation r02):** `find: "apology"` → two occurrences → a 2,000-character window at 83,000 → the apology quoted verbatim from that window, attributed to "the individuals described in subsection (a)" (correct; the RECA findings). Locate-then-read, as the description teaches. The other 5 of 6 C4 rows are nulls at the second call — the pattern O74 names.

**A3, four of four PASS**, and F9 holds in all four: every row names `possibly_superseded`; two tell the asker to retrieve the listed law. Content intact in all four (mention ≠ amendment; one in seven). **A4, four of four PASS** with the recall gap and its figure. **D2, four of four PASS with zero tool calls.** **D1 floor, two of two PASS** ("the appendix no longer exists in the current edition"); the isolation rows are both nulls.

**Isolation C1 r01, PASS but the E17 shape:** the public PDF link and the true size, and also `start_char=50000` relayed to the asker, a `find` recipe, and the API-host `pdf_link` printed beside the public one. Not a fail under version 5 (the public link is there and the path is actionable); exactly what E17 counts.

**Watch item, logged for a preregistration, not ordered: a consumer wanted a locate-only call and the contract refuses it.** Isolation C4 r01 called `get_us_code_section` with `find: "apology"` and `max_chars: 0` — the natural spelling of "find, don't read" — and got `invalid_argument` (WO-11 part D, from S17 finding 4), then repeated with `max_chars: 20000`, then nulled. `max_chars: 1` is the spelling that works and nothing says so. One sample.

**Tool internals in answers: 13 of 37 scorable rows** (O75), concentrated as before on A3 (4 of 4) and C1.

## Run 20260921T233213Z-e22-e17 — E22 (banner carriers) and E17 (the audience sentence), C1 × 10 repeats × 8 cells

Manifest version 6 (`58df7961…`), server `8c3e481`, `openai/gpt-oss-120b`, `loop-scaffold@1`, harness with the null-final fix and pre-turn replacement; 80 rows, 0 provider mismatches, no voided cell, no budget stop, $0.107. An earlier start of the same command was aborted at 12 rows and deleted; nothing from it is counted. **Arm validity, from the traces:** every truncated response in the two `field` cells carries `text.banner` and a `text.content` that does not open with `[WINDOW`; in the two `content` cells the reverse; in `both` cells and the two E17 cells both carriers. `banner-leads-truncated-content` fails in exactly the two `field` cells (the arm's signature, as preregistered) and passes in the other six. **Set aside, 4 of 80:** `e22-floor-field` r01 and r07 and `e22-floor-both` r06, null finals that survived the harness's replacement attempts (four, two and two), and `e17-deepinfra` r03, a `length` finish. 76 scorable rows; distinct answers per arm equal scorable rows in every `temperature: 1.0` cell, and 7 of 9 in `e17-deepinfra`, which has no sampling knob and replayed two draws (r01 = r06, r07 = r10), as SUITE-AUTHORING warned.

**How the version-3 contents clause was applied** (pinned in this session before the tabulation, from the clause's own text — "an excerpt described as N characters is N characters; a cut is marked as a cut"): an answer that points the asker at "the excerpt shown by the tool", "the response above" or "the tool output" without reproducing it FAILS (F7's own case); an answer that opens "Here is the full text of Public Law 118‑31" and then presents no text — only a link — makes a false claim about its own contents and FAILS; the same opener followed by a pasted excerpt labelled as an excerpt does not, because what is shown is described truthfully; a paste that stops early with a marked cut passes. Under the stricter alternative — the opener fails whatever follows — the floor would read `both` 4, `field` 2 and rule (ii) would fire instead of rule (i); that sensitivity is recorded so nobody thinks the outcome is robust to it.

**E22 measures, distinct answers among scorable rows (= rows here):**

| measure | floor both (9) | floor field (8) | floor content (9) | iso both (10) | iso field (10) | iso content (10) |
|---|---|---|---|---|---|---|
| M1 window-as-law or wrong total | 0 | 0 | 0 | 0 | 0 | 0 |
| M2 PASS under version 3 | 6 | 6 | 5 | 4 | 4 | 2 |
| M3 `[WINDOW chars` in the answer | 4 | 0 | 1 | 2 | 0 | 0 |
| M4 `start_char` in the answer | 5 | 1 | 3 | 4 | 1 | 1 |
| M5 public PDF URL | 9 | 8 | 9 | 10 | 10 | 10 |

**Decision, by the rule pinned in E22: (i) fires — the content line goes.** `field` is no worse than `both` on M1 (0 = 0) and M2 (6 = 6 floor, 4 = 4 isolation) in both roles. `content` is worse than `both` on M2 by 2 in isolation, so (iv) does not apply and the choice is not on design grounds alone. Consequence, as pinned: `text.content` becomes pure payload, the R13c coordinate sentence is retired, the banner check is repointed at `text.banner` in the next manifest version, and the switch is removed. Contract in `40-tools.md`; build order WO-13.

**The expectation was wrong where it mattered, and the maintainer's reading was right (F13).** This session expected M4 not to differ by arm, on the theory that the relayed continuation comes from `text.message`, which every arm carries. Measured: `start_char` reaches the asker in 5 of 9 and 4 of 10 under `both`, and 1 of 8 and 1 of 10 under `field` — lower by four and three, past the preregistered two. M3 says why: the banner line is quoted into the answer in 4 and 2 `both` rows and in no `field` row. When the banner sits inside `text.content` the model pastes the content, banner first, and the continuation offset comes with it; with the banner in a sibling field it pastes the payload and the offset stays behind. The message's own continuation sentence is relayed far less than the banner's. The parroting the maintainer saw on `gpt-oss-120b` was the content line's. Recorded as the preregistered falsifier firing, in the direction against this session.

**E17: confirmed on both arms, by a margin the preregistration did not anticipate.** DeepInfra arm (O51's knobs exactly): distinct answers giving the asker the public PDF URL inline, **7 of 7**, against 0 of 6 at O51c/d; distinct answers relaying `start_char`, 3 of 7, below half as expected. AkashML arm: 10 of 10 with the URL, against 3 of 7 with the URL unpinned at O51c; `start_char` in 3 of 10. Both pinned arms moved the same way, so the confound falsifier (a provider effect) does not fire: the audience sentence in `text.message` (WO-6/WO-7) is what changed. Not expected to change and did not: the self-truncation — every pasted window in the run is cut short by the model with a marker. One row, `e17-deepinfra` r03, cut at the 4,096 cap and set aside, against three of ten at O51b.

**F12 (from every arm) — consumer-behavior finding: "Here is the full text of Public Law 118‑31", followed by no text.** 48 of 76 scorable answers open with that sentence or its twin ("Below is the complete text…"); in 34 of them nothing but a link follows. It is the one phrase behind most M2 fails, and it is a sentence about the answer that the answer then contradicts. By arm: `both` 9 of 19, `content` 15 of 19, `field` 15 of 18 — an arm that pastes the content line pastes text under the opener, which makes it true-ish; an arm that does not paste leaves the opener bare. Related, in 6 rows across five arms: **the default read as a ceiling** — "the tool can only return a 20,000‑character window at a time" (`e17-akashml` r02, the maintainer's example), "at most ~20 KB", "the tool truncates large documents" — while other rows in the same run pulled 200,000 in one call. The description says "defaults to 20,000 … pass a larger value explicitly if your host delivers it"; a weak model turned the default into a limit. Both are the model describing the tool wrongly to the person, not relaying anything the tool said. No tool-side change is proposed for the opener; for the ceiling claim, a rewording of the default sentence is a candidate for the next description run under R20(b), not ordered.

**C1 at the floor, for the record:** 14 of 19 scorable `both`-carrier floor answers give the asker the public PDF and the true size and do not present a window as the law — the F1 failure did not appear once in 76 rows. What remains on C1 is the contents clause, and it is now mostly F12.

## Run 20260922T120550Z-e22-secondary — E22's secondary read: C2, C3, C4 once in each of the six arms

Manifest version 6, server `8c3e481`, 18 rows, 0 provider mismatches. This read had been withdrawn after O76 (the decision rests on C1, and the arms cease to exist under WO-13); the maintainer ran it anyway and it was preregistered, so it is scored. It changes nothing about the E22 decision — at one row per arm it cannot — and is recorded for what it adds to F11, C4 and the locate-only question. **Set aside, 8 of 18:** C4 in all six arms, null finals; `e22-floor-both` C3 and `e22-floor-field` C2, pre-turn failures the harness replaced three times and once without success (no scored turn, empty answer). **Scores, 10 scorable:** C2 — PASS in all five scorable arms (the four factors quoted exactly from retrieved text, cited); C3 — `floor-field`, `isolation-content`, `isolation-field` PASS (every section number they cite is in the retrieved `find` snippets or window), `floor-content` **FAIL** and `isolation-both` **FAIL**.

**F11, two more instances, one of each species.** `floor-content` C3 read the first 20,000 characters with no `find`, told the asker to search the PDF for "AUKUS" themselves, and then supplied what "the language typically reads something like" — a hedged, invented quotation, with "summary from the full text" as its heading; the fabricated-quotation species from O75. `isolation-both` C3 ran `find`, cited the real sections correctly, and then filled a table with ranges that exist in nothing retrieved — "Sec. 1334‑1336 Implementation plan", "Sec. 1353‑1355 Task Force on AUKUS", "Sec. 1360‑1363", "Sec. 1364‑1366" — the invented-section-numbers species from O68. Across the four loop runs C3 is now 5 PASS, 4 FAIL, 4 set aside, and every FAIL is section numbers or quotations that were never retrieved. `isolation-content` passes with the same watch item as before: `find` offsets printed as section numbers ("Sec. 1294068‑1294277").

**C4: null in 6 of 6 arms; 11 of 12 across the four runs.** The `floor-both` row is the sharpest instance: five calls — search, a 20,000-character read, `find: "apology"`, then windows at 81,700 and 82,400 that contain the apology — and then a contentless final. The consumer had the answer in hand and lost it at the sixth step. E25's question (pin, knob, or the model) is the right one; nothing in these rows implicates the server, whose every response was correct.

**Locate-only, second instance:** `floor-field` C3 called `get_public_law` with `find: "AUKUS"` and `max_chars: 0`, was refused (`max_chars must be > 0, got 0`), and retried with `max_chars: 1` successfully. Two consumers in two runs have spelled "find, don't read" as `max_chars: 0` (O75 was the first). Decided as technical judgment (below, `40-tools.md`): the refusal stays and its message teaches the spelling that works.

## Run 20260923T021835Z-e25 — E25: is the contentless final the pin, the knob, or the model?

Manifest version 6, server `8c3e481`, four isolation cells × A2/C4/D3 × 10 repeats = 120 rows, $0.083, no voided cell, no pre-turn (isolation). **Set aside as instrument, 5 rows:** HTTP 429 from the endpoint on three consecutive requests — `e25-akashml-t1` A2 r03, C4 r05, C4 r08, D3 r03 and `e25-deepinfra-t1` C4 r08 — recorded in the row's `error_responses`, answer empty, no `consumer_limit` cause; a rate limit is not a consumer outcome. (Note for the harness: an empty answer with `consumer_limit: null` reads as a null final unless `error_responses` is checked; a cause of its own would be cleaner.) Provider verified on every request that carried a provider name; 0 mismatches. The two `notemp` arms did not replay: 10 distinct answers of 10 on every prompt in `akashml-notemp`, and in `deepinfra-notemp` 9 of 9 non-null on D3.

**Null finals (`consumer_limit.cause: null_final_content`), by arm, over rows that were not rate-limited:**

| arm | A2 | C4 | D3 | total |
|---|---|---|---|---|
| `deepinfra/bf16`, temperature 1.0 | 5/10 | 6/9 | 1/10 | **12 of 29** |
| `deepinfra/bf16`, no temperature | 5/10 | 9/10 | 0/10 | **14 of 30** |
| `akashml/bf16`, temperature 1.0 | 0/9 | 0/8 | 0/9 | **0 of 26** |
| `akashml/bf16`, no temperature | 0/10 | 0/10 | 0/10 | **0 of 30** |

**Decision, by the rule pinned in E25: (i) fires — a provider effect, and only a provider effect.** Each AkashML arm is at least 8 rows below its DeepInfra partner (14 and 12 below); the temperature arms differ from their partners by 2 and 0, ties. `deepinfra-t1` at 12 of 29 reproduces O74/O75 (the falsifier of the instrument did not fire). **This session's expectation — that temperature would matter more than the pin — is falsified**; the pin is the whole effect, on this model, this scaffold and these prompts. The mechanism reading from O74 (position in the turn) survives in a sharper form: on DeepInfra the rows that nulled are the ones that reached a second or third call (18 of 25 nulls with a recorded step at step 3, 7 at step 2, none at step 1), and on AkashML the same prompts went to 2–5 calls and finished — C4 answered in 18 of 18 non-limited rows, 17 of them quoting the apology verbatim from a retrieved window. The lost call is lost between the model and the caller on one provider's serving stack; the wire lines cannot say which layer, as preregistered.

**Consequence: repinning is now a question with a measured answer behind it — Q20.** The cost is as S23 stated: a new `cell_id`, so rows on a new pin do not pool with the two complete runs, E22, E17 or O51. The benefit is measured here: on multi-step prompts the DeepInfra pin loses 40–90% of rows, the AkashML pin lost none in 56, and C4 — null in 11 of 12 rows across every earlier run — passes 17 of 18 on AkashML by locate-then-read.

**C4 scored in passing, since the rows exist:** 21 of 22 non-null C4 rows across the four arms quote the apology verbatim from a retrieved window after a `find` (the one that does not is `akashml-t1` C4 r02, a single call with no `find`, not scored here). The prompt is not hard for this model; it was hard for this pin.

## Run 20260923185503-loop-complete-v7 — the first complete run on manifest version 7: the AkashML pin and the second lineage

Manifest version 7 (`66b9a347…`), server `0436218` (WO-13; the first run where `text.banner` is the only carrier), harness 981a62f, `--repeats 2` over `loop-floor`, `loop-isolation` (both `openai/gpt-oss-120b @ akashml/bf16`, the first run on the pin) and `loop-floor-deepseek` (`deepseek/deepseek-v4-flash @ gmicloud/fp8`, the first run of a second lineage): 78 rows, $0.142 ($0.049 / $0.015 / $0.077), 0 provider mismatches, no voided cell, no budget stop, all 17 checks pass in every cell (`private-law-scope-not-notfound` is vacuous in the two gpt-oss cells, which answered D2 from the description without a call, and non-vacuous for the first time in a loop run in the DeepSeek cell). **Null finals: 0 of 78.** Contentless rows on the retired pin were 22 of 78 (O74) and 8 scored-turn nulls of 52 (O75); here the E22 set-aside test — `content: null`, no tool call — fires on no row. **Set aside, 2 rows, both `loop-floor`:** A4 r02, exhausted after four attempts each ending in HTTP 429 from AkashML (`upstream_unavailable`; 12 rate-limited requests in all, at steps 1, 4, 2 and 1); and C4 r01, whose answer is a tool call in the model's raw channel syntax (F14 below) — not an answer, and not the E22 shape either. Three other floor slots (C1 r01, C4 r02, D2 r01) lost an attempt to a 429 and were replaced by the harness's retry; their scored rows are the replacements. AkashML rate-limited 7 attempts in this cell and none in the other two.

**Scores, 76 scorable rows: floor 20 PASS / 4 FAIL of 24; isolation 22 PASS / 4 FAIL of 26; DeepSeek floor 26 PASS / 0 FAIL of 26.** The eight gpt-oss failures are A2 and A5, every row, both cells. Not pooled with O68/O75 (different `cell_id`); stated beside them: floor 19 of 26 and isolation 25 of 30 there, on rows a quarter of which the old pin destroyed.

| | floor r01 | floor r02 | isolation r01 | isolation r02 | deepseek r01 | deepseek r02 |
|---|---|---|---|---|---|---|
| A1 | PASS | PASS | PASS | PASS | PASS | PASS |
| A2 | **FAIL** | **FAIL** | **FAIL** | **FAIL** | PASS | PASS |
| A3 | PASS | PASS | PASS | PASS | PASS | PASS |
| A4 | PASS | 429 | PASS | PASS | PASS | PASS |
| A5 | **FAIL** | **FAIL** | **FAIL** | **FAIL** | PASS | PASS |
| C1 | PASS | PASS | PASS | PASS | PASS | PASS |
| C2 | PASS | PASS | PASS | PASS | PASS | PASS |
| C3 | PASS | PASS | PASS | PASS | PASS | PASS |
| C4 | raw call | PASS | PASS | PASS | PASS | PASS |
| D1 | PASS | PASS | PASS | PASS | PASS | PASS |
| D2 | PASS | PASS | PASS | PASS | PASS | PASS |
| D3 | PASS | PASS | PASS | PASS | PASS | PASS |
| D4 | PASS | PASS | PASS | PASS | PASS | PASS |

**C4 is decided by the pin, as O78 predicted from E25's C4 rows: 5 of 5 scorable PASS**, against 1 scorable pass in 12 C4 rows across the DeepInfra runs. Every passing row is locate-then-read — `find: "apology"` (or `"apolog"`), two or three occurrences, a window at 83,000 (or 81,800), the operative sentence quoted verbatim — the pattern the description teaches and E14 was built to measure. The prompt was never hard for this model; the pin lost the second call.

**F14 (from floor C4 r01) — the lost tool call, seen from the other provider.** The row's final assistant message has `finish_reason: stop`, 75 completion tokens, reasoning "We need note about apology. Search within notes for 'apology'," and as its `content` the literal text `<|start|>assistant<|channel|>commentary to=functions.mcp__uscode_mcp__get_us_code_section{"granule_id": …, "find": "apology"}<|call|>` — a well-formed tool call in the model's own channel markup, passed through as prose instead of parsed into `tool_calls`. This is the event E25 could not see: on DeepInfra the same model-side output came back as `content: null` (the parser dropped it), on AkashML it comes back verbatim. One row of 52 gpt-oss rows here, against 26 of 59 on DeepInfra (O78) — the rates differ by an order of magnitude, so the serving stacks differ in more than their parser, but the mechanism reading (O74, O78: a formatting failure at a second or third call) now has a specimen. Set aside as a consumer limit; for the harness, a cause of its own (`raw_tool_call_in_content`) would let the set-aside test catch it — as written, the row reads as an answered row with 191 characters.

**F8, now 7 of 7 gpt-oss A2 rows across runs; the second lineage passes both.** All four gpt-oss rows quote "(b) the nature of the copyrighted work;" as the exact text of 17 U.S.C. 107(b) — the second factor relabelled with the asked-for designator, the same sentence in three rows and the whole section under a "107(b)" heading in isolation r02. Two of the four called with `citation: "17 U.S.C. 107(b)"` and had `stripped_subsection: "(b)"` in the response; two called for 107 itself. DeepSeek's two rows open "17 U.S.C. § 107 has no subsection (b)" and quote the whole section, one of them citing `stripped_subsection` to the asker by name. The designator-count preregistration (F8) stands as the next one; it now has a contrast model.

**F10, now 9 of 9 gpt-oss A5 rows across runs, and a new fact: the `ambiguous` response reaches the model and is overridden.** Floor r02 and isolation r02 called the citation, received `ambiguous` with both candidates (count 2), re-requested the first candidate by `granule_id`, and presented Appellate Rule 9 as if unique — the disambiguation the contract serves was read, acted on, and withheld from the asker. Floor r01 and isolation r01 took the search path (count 2, or 4,153 hits for the bare phrase) and picked the first hit. The disposition proposed at O75 — a same-citation note on search hits — would reach the first pair and not the second; the second pair says the by-id follow-up needs to carry the collision forward, or nothing the server serves will. DeepSeek: r01 stops at `ambiguous` and asks which of the two (naming both by rule set and granule); r02 fetches both and presents both. 2 of 2 PASS.

**C1, four of four gpt-oss rows PASS under the version-3 clause as applied at run 20260921T233213Z (a paste that stops early with a marked cut passes), and F7(i) persists on the new pin.** Every gpt-oss row states the true total (3,590,552), gives the public PDF link, and pastes a fenced excerpt cut with an ellipsis — and every one describes the excerpt as the whole window: "first 20,000 characters" over pastes of 10,923 (55%), 6,688 (33%) and 7,986 (40%) characters, and "first 50 000 characters" over 7,970 (16%) in isolation r01, which also opens "Here is the full text" before qualifying. Floor r02's 6,688-character paste is the O51a cut point again (6,717 of the window), so the self-truncation is the model's at this setting whatever the provider, as O51a said. Two rows relay `start_char` to the asker beside the link (the E17 shape; not a fail). DeepSeek pastes nothing: both rows give the size, the PDF link, what the first window contains, and an offer to fetch parts or `find` — the shape the clause was written to reward, and the first C1 rows without a paste.

**C3, six of six PASS; section-number confabulation logged twice more.** Floor r01 presents `find` offsets as section numbers ("Sec. 1294068‑1294626", "Sec. 1305736‑1305877") with glosses drawn from the snippets at those offsets; isolation r01 assigns real provisions to sections that are not theirs ("Sec. 1360‑1363" the implementation plan, "Sec. 1366‑1369" the AUKUS caveat, "Sec. 1370‑1372" expedited export review) — the subtitle runs Secs. 1321–1354 (verified against the law's own table of contents, fetched keyless: `runs/2026-09-23-manifest-v7/PLAW-118publ31.htm`), and the provisions named live in Secs. 1331, 1332 and 1344. Scored PASS because every provision asserted is in the retrieved snippets or table of contents and no quotation is invented (the F11 line), with the sensitivity recorded: under a reading that a section number is itself a claim, isolation r01 fails. Three gpt-oss C3 rows across runs now carry wrong section numbers; none carries an invented quotation since F11's second species (O75). DeepSeek's two rows read the subtitle itself (windows at 1,288,950–1,366,770) and report it section by section, correctly numbered — the (a) pass at full strength, 5 and 7 calls.

**A3, six of six PASS; the E26 baseline's first four rows.** All four gpt-oss rows disclose the edition and `currentthrough`, name Public Law 119-74 as a later law that mentions the section, and say in words that a mention is not an amendment; none carries the one-in-seven figure; none reads the listed law. Identifier relay (E26 measure (a), identifiers inside URLs excluded — see the E26 amendment): 3 of 4 — `possibly_superseded` and `laws_indexed` as field names in floor r01, r02 and isolation r02; none names `get_public_law` or a `package_id`, which O67b's floor row did. Isolation r01 relays nothing. DeepSeek, not an E26 arm but the contrast: both rows call `get_public_law` on Public Law 119-74 with `find: "2210"`, report that its one mention is a parenthetical in a loan-guarantee rider and that it amends nothing, and carry the one-in-seven caveat — the pinned instruction followed to the letter, in prose, with no field name. Relay 0 of 2 on the E26 list (a granule id in backticks in r02, outside it).

**WO-13 part D, first live effect: isolation C3 r02 asked `find: "AUKUS"` with `max_chars: 0`, was refused, and re-asked with `max_chars: 1`.** One consumer, one occurrence, the exact repair the refusal teaches (O79d); the O75 watch item that produced part D is answered on its first appearance.

**The banner as a field only: 0 of 78 answers quote the banner line** (E22's M3 was 0 of 18 under `field`; this is the complete-run confirmation on both lineages). The two payload checks pass on all 132 truncated records.

**Tool internals in answers (O67b's rule, identifiers inside the public PDF link excluded): floor 5 of 24 scorable, isolation 7 of 26, DeepSeek 4 of 26** — 12 of 50 gpt-oss rows (24%) against 13 of 37 (O75); concentrated on A3 (field names), A4 (the `uscodecitation:` query string, 3 rows), C1 (`start_char`, 2 rows) and D2 (`get_public_law` named in the refusal, 2 rows). DeepSeek's four are `stripped_subsection` (A2 r02), granule ids in backticks (A3 r02, A5 r01) and the query string (A4 r02).

**Watch items, logged, not scored.** (1) A1: note contents invented from headings — floor r01 (5,000-character window, `structure` only) says the Short Title note assigns "Indemnification and Limitation of Liability" and that References in Text cite "the Nuclear Regulatory Commission's authority"; isolation r02 says the Short Title is "Radiation Exposure Compensation Act". The short title note says Price-Anderson Act (DeepSeek read it). A1's criteria do not cover invented note contents; a version-8 clause is the fix if a third row does it. (2) D3, DeepSeek: priors as decoration — "Title 17 is the copyright title (17 U.S.C. §§ 101–1332, roughly)" and "13 chapters and roughly 1,400 sections" (Title 17 has 15 chapters, through §1511); no fabricated provision at 9999, so a pass. (3) D4, DeepSeek r02: a visible self-correction in the answer ("was later amended by the Fairness in Orphan Works…? No — I shouldn't speculate"). (4) A4, DeepSeek: 22 and 25 tool calls in 11 and 6 requests, no step cap hit, $0.018 and $0.008 — the second lineage reads every listed law with `find` and separates amenders from citers, and r01 documents a real recall-gap case (Pub. L. 109-295 amends the section by an editorial name change and never says "2210"). (5) D1, DeepSeek: both rows go past the zero-hit to the 2014 edition with `historical: true` and serve the eliminated section's text labelled as historical — the bonus the criteria name.

**The second lineage, read as an instrument.** 26 of 26, every call count sensible, every claim traceable, no null, no rate limit; it passes the two prompts gpt-oss fails deterministically and reads the listed law on A3 unasked. It is three times the cost per row of the gpt-oss floor and takes 4–25 calls where gpt-oss takes 1–3. It is not pooled with anything; one run, two repeats. Whether it gates is Q21.

## Run 20260923200847-e26-before — E26's baseline top-up: A3 × 8 repeats under `loop-floor` and `loop-isolation`

Manifest version 7 (`66b9a347…`), server `0436218` (the pre-WO-14 caveat), same `cell_id`s as the complete run: 16 rows, $0.020, 0 provider mismatches, all checks pass or vacuous. AkashML rate-limited 5 attempts, all in `loop-floor` (r01 lost three, r04 two), all replaced by the harness; none in isolation. **Set aside, 1 row: isolation r05, the second F14 instance** — the answer is `<|start|>assistant<|channel|>commentary to=functions.mcp__uscode_mcp__get_us_code_section{"citation": "42 U.S.C. 2210", "max_chars": 10000}<|call|>`, `finish_reason: stop`, 56 completion tokens, 7 of reasoning ("We need to fetch section."), no tool call recorded, no retrieval at all. This one is at step 1, before any tool result, so O74's position reading (the lost call is a second or third call) does not cover it; F14 is now 2 of 68 gpt-oss rows on this pin. **A3: 15 of 15 scorable rows PASS** — every answer discloses the 2024 edition and 2025-01-06, names Public Law 119-74 as a later law that mentions the section, and says a mention is not an amendment.

**The E26 baseline, complete: 20 rows asked, 19 scorable (10 floor, 9 isolation).** Measures as preregistered and amended (identifiers inside URLs excluded; (c) and (e) read by hand):

| measure | floor (10) | isolation (9) | total (19) |
|---|---|---|---|
| (a) relay — any identifier from the list | 10 | 8 | **18 of 19** |
| (a′) relay as a call recipe — a tool name or `package_id` | 1 | 2 | 3 of 19 |
| (b) read — `get_public_law` on the listed law in the trace | 0 | 0 | **0 of 19** |
| (c) plain disclosure — a later law mentions the section and was not checked | 10 | 9 | 19 of 19 |
| (e) substance — a mention is not an amendment | 10 | 9 | 19 of 19 |
| (e) substance — the one-in-seven figure | 0 | 0 | 0 of 19 |
| incompleteness of the index stated in words, no figure | 1 | 1 | 2 of 19 |

What is relayed is the object's field names — `possibly_superseded` in 15 of 19, `laws_indexed` in 14, the package id in backticks (`USCODE-2024-title42`) in 9, `start_char` in 4 — and in 3 rows the recipe itself: floor top-up r04 numbers the steps for the asker ("Retrieve the full text of Public Law 119-74 (`get_public_law` with `package_id: "PLAW-119publ74"`)"), isolation r01 and r04 name `get_public_law` as the way to read the law. The only row that relays nothing is the complete run's isolation r01. One phrase for the record: isolation r06 calls the object a "supersession check" — the model's own name for `possibly_superseded`, which no served string uses — and quotes the field and value back on the next line. The instruction the caveat gives is followed by no row and relayed by most; the substance it carries survives in every row. That is F9 at n=19, and the shape E26's after-arm is measured against: rule (i) needs relay at or under 9 of the after-arm's scorable rows with substance within 2 of 19.

**Instrument check, as preregistered:** 10 of 10 floor and 9 of 10 isolation rows answered and not rate-limited, above the 6-of-10 falsifier; the pin is not the story. **F7(i) again, in passing:** 11 of 15 top-up answers paste a fenced excerpt and describe it as "the first 20 000 characters" or similar; two say the tool returned 6,000 or 10,000 characters when it returned 20,000. Not scored on A3.

## Run 20260924T010948Z-e26-after — E26's after-arm: A3 × 10 repeats under `loop-floor` and `loop-isolation` at the WO-14 caveat

Manifest version 7 (`66b9a347…`, the baseline's hash), server `e0165f5` (WO-14, verified O83), run by this session with the harness's key: 20 rows, $0.028, 0 provider mismatches, all checks pass or vacuous, **0 null finals, 0 raw-channel tool calls, 20 of 20 scorable.** AkashML rate-limited 6 attempts (5 in `loop-floor`, 1 in isolation), all replaced; one floor attempt ended on a contentless crowding pre-turn and was replaced (`attempts_unmet` 1). **A3: 20 of 20 PASS.**

**E26, scored by the rule pinned before any row existed (with the 2026-09-23 amendment):**

| measure | before (19 scorable) | after (20 scorable) |
|---|---|---|
| (a) relay — any listed identifier outside a URL | 18 | **16** |
| (a′) relay as a call recipe — a tool name or `package_id` | 3 | **0** |
| (b) read — `get_public_law` on the listed law | 0 | 0 |
| (c) plain disclosure — a later law mentions the section and was not checked | 19 | 20 |
| (e) substance — a mention is not an amendment | 19 | 20 |
| (e) substance — the one-in-seven figure | 0 | 0 |
| `start_char` relayed to the asker | 4 | 0 |
| `possibly_superseded` named as a label | 15 | 16 |
| `laws_indexed` named | 14 | 10 |
| a package id in backticks | 9 | 6 |

**Rule (ii) fires.** Rule (i) needed relay at or under 9 of 20; it is 16. Substance survival is within 2 (20 of 20 against 19 of 19), so rule (iii) does not fire. As preregistered, (ii) reads: the audience sentence is not the lever for this instruction as it was for WO-6's; the caveat reverts to the pre-WO-14 wording and the question goes back to the maintainer with the rows. **The rows, before the revert is ordered, because they say something the rule's single number does not:** what the redraft removed is exactly the species F9 was written about — no row now tells the asker to call `get_public_law`, none names a `package_id`, none hands over `start_char` (3, 3 and 4 rows did before). What it did not touch is the object's *name*: 16 rows still write "The tool's `possibly_superseded` indicator shows…" and 10 quote `laws_indexed`, as a label for the thing they are explaining, in the same proportion as before (15 of 19). Every row then explains it correctly in prose — a later law mentions the section, the index does not say whether it amends, the law would need to be read — and no row reads it. So the sentence "do not pass on tool names, package ids or field names" was followed for tool names and package ids and not for the two field names the model uses as the noun for the check itself. The recipe species is gone (3 → 0 of 20, p ≈ 0.10 by Fisher — a small count, but the direction is the one asked for); the label species is unmoved; the read rate is unmoved at zero. Q22 puts the revert to the maintainer with this table; until it is answered the served wording stays WO-14's, since the rows show it no worse than the old one on any measure and better on three.

**Isolation C3 r02's lesson generalised, in passing:** the after-arm's answers contain no `start_char` at all where the baseline's had four, on a build that changed nothing in `text.message`. One run; noted, not claimed.

## Run manual-interview-test-crowded — one A2 row on manifest version 8 and the first interview (instrument test, not pooled)

Manifest version 8 (`1286c419…`), `loop-floor` at `akashml/bf16`, crowded, one row, run by the maintainer 2026-09-24 to test the harness's interview mode (7a26eb2); not a suite run and pooled with nothing. Read in full as O85; the model's self-report is S25. **A2: FAIL** — quotes factor (1) under a "107(b)" heading (the version-7 rows quoted factor (2)); called 107 itself with `find: "(b)"`, so no strip message was served and the five hits, all in the notes, were ignored. **F8, now 8 of 8 gpt-oss A2 rows across runs.** The interview put one question with a false premise of its own ("it is (2)") and the model accepted it, switched sentences, and called the change a wording slip. F8's preregistration is amended by O85(c): the designator count must also be reachable through `find`, since a consumer that strips the subsection itself never triggers the strip path, and the measurement carries a relabel/misattribution species split. The class-1 standing of the relabel species is with the maintainer; this session's recommendation (keep it, let F8 measure) is in O85(c).
